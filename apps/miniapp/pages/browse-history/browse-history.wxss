/* pages/browse-history/browse-history.wxss */
.hs-day {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--muted);
  padding: 4rpx 0 16rpx;
}

.hs-item {
  display: flex;
  align-items: center;
  gap: 20rpx;
  padding: 20rpx 0;
  border-bottom: 1rpx solid var(--line-faint);
}
.hs-item:last-child { border-bottom: none; }

.hs-ic {
  flex-shrink: 0;
  width: 80rpx;
  height: 80rpx;
  border-radius: var(--r);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 38rpx;
}
.hs-ic.tone-teal  { background: var(--teal-wash);  color: var(--teal-deep); }
.hs-ic.tone-wheat { background: var(--wheat-wash); color: var(--wheat-deep); }
.hs-ic.tone-clay  { background: var(--clay-wash);  color: var(--clay-deep); }

.hs-body { flex: 1; min-width: 0; }
.hs-t {
  font-size: 27rpx;
  font-weight: 600;
  color: var(--ink);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.hs-sub {
  margin-top: 8rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: 22rpx;
  color: var(--muted);
}
.act-tag {
  display: inline-flex;
  align-items: center;
  height: 34rpx;
  padding: 0 12rpx;
  border-radius: var(--r-full);
  font-size: 21rpx;
  font-weight: 600;
}
.act-tag.at-view { background: var(--teal-wash);  color: var(--teal-deep); }
.act-tag.at-jump { background: var(--wheat-wash); color: var(--wheat-deep); }
.hs-src { color: var(--faint); }

.hs-time {
  flex-shrink: 0;
  font-size: 22rpx;
  color: var(--faint);
}

.hs-del {
  flex-shrink: 0;
  height: 56rpx;
  padding: 0 22rpx;
  display: flex;
  align-items: center;
  border-radius: var(--r-full);
  font-size: 23rpx;
  font-weight: 600;
  color: var(--error);
  background: var(--error-soft);
}

/* 登录引导条 */
.hs-bar {
  display: flex;
  align-items: center;
  gap: 20rpx;
  margin-bottom: 20rpx;
  padding: 24rpx 26rpx;
  border-radius: var(--r);
  background: var(--teal-wash);
  border: 1rpx solid var(--line-faint);
}
.hs-bar-body { flex: 1; min-width: 0; }
.hs-bar-t { font-size: 26rpx; font-weight: 600; color: var(--ink); }
.hs-bar-d { margin-top: 6rpx; font-size: 22rpx; color: var(--muted); line-height: 1.5; }

/* 数据归属说明：本机 / 账号必须写清楚 */
.hs-note {
  margin-top: 20rpx;
  padding: 0 8rpx;
  font-size: 22rpx;
  color: var(--muted);
  line-height: 1.6;
}
.hs-clear { margin-top: 20rpx; display: flex; justify-content: center; }

.empty-slot {
  padding: 60rpx 20rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}
.empty-slot.compact { padding: 38rpx 20rpx; }
.es-ic { font-size: 88rpx; color: var(--faint); margin-bottom: 20rpx; }
.es-t { font-size: 30rpx; font-weight: 600; color: var(--ink-soft); }
.es-d { margin-top: 8rpx; font-size: 24rpx; color: var(--muted); }
.es-actions {
  margin-top: 26rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-wrap: wrap;
  justify-content: center;
}
