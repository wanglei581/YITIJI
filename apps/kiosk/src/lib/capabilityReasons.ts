// ============================================================
// 能力门禁置灰原因文案（KIOSK-DEBT）。
//
// 一体机是 27 寸竖屏触摸屏，**没有 hover**：`title` 属性永远不会显示。
// 能力门禁（功能不可用）置灰时，原因必须是**常显文字**，不能靠 title。
// 瞬时态（提交中 / 加载中）不属于这里，原生 disabled 即可。
//
// 文案原则：
//   - 具体优于笼统：「该来源未提供投递链接」好过「当前不可用」；
//   - 拿不到具体原因时说「暂时不可用」，**不编造**一个具体理由；
//   - 只解释「为什么不能点」，不承诺「什么时候能点」。
// ============================================================

/**
 * 岗位 / 招聘会参展企业的来源投递链接不可用。
 *
 * 判据是 `isValidSourceUrl(sourceUrl)` 为假 —— 来源方同步过来的记录里没有
 * 可用的外部链接。本终端不接收简历、不代投递，没有链接就真的没有下一步，
 * 所以这里只能如实说明，不能给一个「换个方式投」的假出口。
 */
export const SOURCE_APPLY_UNAVAILABLE_REASON = '该来源未提供可用的投递链接，请到来源平台查询该职位'

/** 企业当前没有在招岗位（不是「查不到」，是确实为 0）。 */
export const COMPANY_NO_OPEN_JOBS_REASON = '该企业当前没有在招岗位'

/** 演示模式：没有连真实后端，不会生成真实文件。 */
export const DEMO_MODE_NO_REAL_FILE_REASON = '演示模式未接入后端，无法生成真实文件'

