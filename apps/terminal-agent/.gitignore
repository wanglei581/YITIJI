node_modules/
dist/

# Agent config contains agentToken/terminalId — NEVER commit
config/agent-config.json
agent-config.json
# SQLite state DB (Phase 8.1C+)
data/*.db

# Sample test files — never commit actual documents
samples/*.pdf
samples/*.jpg
samples/*.jpeg
samples/*.png
samples/*.bmp
samples/*.tiff
samples/*.tif
samples/*.txt
