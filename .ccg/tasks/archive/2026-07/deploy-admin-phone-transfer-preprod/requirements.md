# 执行范围

- 真实目标：让已合入 `main@3c5b5a55` 的机构账号手机号安全转移引导在预生产 Admin 可用。
- 允许：构建 Admin HTTP production bundle；备份并替换预生产 `apps/admin/dist`；追加脱敏部署标记；执行 health、静态资源和受控浏览器验收。
- 用户已于 2026-07-26 明确授权：在预生产向 `183****1921` 发送真实验证码，并将该手机号从已停用机构账号安全转移到 `admin` 管理员账号。
- 本阶段允许：使用已恢复的管理员临时密码登录；通过正式 `/auth/password/change` 自助改为强随机长期密码；调用正式手机号转移 start/verify 接口；由既有事务更新 Admin/Partner 手机字段与会话版本；写既有审计。
- 禁止：绕过 OTP、直接 SQL 搬运手机号、修改短信配置、修改机构账号启用状态、修改其他账号、Kiosk、Partner 前端、Terminal Agent 或生产环境。
- 回滚：保留替换前 Admin dist 压缩备份和同目录回滚副本；静态资源或浏览器门禁失败时立即恢复。
- 验收：管理员自助改密后真实登录成功；转移 start 返回唯一来源机构账号并真实下发短信；用户回填 OTP 后 verify 成功；Admin 获得已验证手机号、Partner 释放手机号、双方旧会话失效、审计不含密码/OTP/ticket/手机号明文。
