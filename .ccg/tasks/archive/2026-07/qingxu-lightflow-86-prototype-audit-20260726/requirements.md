# 需求与边界

## 目标

- 读取 `http://localhost:8399/kiosk-proto-2026-07-fusion/index.html` 的完整原型目录。
- 盘点当前仓库 Kiosk、Admin、Partner 每个正式页面及壳层的 LightFlow 迁移状态。
- 识别 Cursor 已做但未完成的候选改动，不直接吸收未知工作区。
- 建立原型、生产路由、页面组件、状态分支和缺口之间的映射。

## 实施范围（2026-07-26 确认）

- 以 `origin/main=9651b109` 为基线，不重写已合入的 87 路径页面。
- 仅收口活动详情、法务文档、百宝箱三页共享页壳/页头，并补齐对应静态与浏览器门禁。
- 不修改 API、DTO、Prisma、权限、认证、支付、打印、扫描、AI、TRTC 或 Terminal Agent。
- 不把静态原型的示例数据、演示登录、本地假状态迁入生产。
- 不修改 `ProfilePage`、`/me/*` 生产页面；`/me/privacy-requests` 只作为已合入的第 87 条路由验证。
- Admin、Partner 继续保持最新主线 Inkpaper 主题，本任务不改。

## 后续实施原则

- 保持真实数据、加载、空态、错误、重试、权限、禁用、返回路径和现有入口。
- 视觉与交互可按原型统一，但业务契约和合规语义以当前代码与正式文档为准。
- 按波次、文件所有权和 TDD verify 推进，不一次性铺开全部页面。
