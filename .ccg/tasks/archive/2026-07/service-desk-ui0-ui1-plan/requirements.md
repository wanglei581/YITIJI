# Requirements

- 只制定蓝白服务台 UI-0/UI-1 第一批实施计划，不修改页面代码。
- 第一批固定为共享视觉基础、三端壳层、Kiosk 首页、Admin 工作台、Partner 岗位管理。
- 计划必须列明文件预算、非目标、TDD 顺序、验证命令、真实浏览器验收、证据等级和逐批回滚方式。
- 不触碰 API、认证、支付、打印、扫描、AI、TRTC、数据库、Terminal Agent 或其他并行任务。
- 计划提交后停止，等待用户明确批准实施。
