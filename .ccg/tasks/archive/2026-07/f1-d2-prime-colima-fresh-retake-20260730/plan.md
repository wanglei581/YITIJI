# F1 D2′ Colima Fresh Retake 执行计划

1. 确认隔离 worktree、精确候选提交、授权窗口、全新 evidence 路径和 production 禁止边界。
2. 运行本机 offline contract、Shell/Node 语法与 API build 基线；Claude、Antigravity、Cursor 并行只读复核执行闸门。
3. 启动现有 Colima，只读核验 Linux/systemd/linger/cgroup、版本、生产变量 denylist、端口、旧 evidence 和残留进程。
4. 在 guest ext4 建立全新 detached `e721f87a` 隔离副本，确认无 `.env`、Git 干净，执行 frozen install、fresh API build 与 offline contract。
5. 在授权窗口内使用精确 evidence/work 目录执行唯一一次 `drill:d2-same-host`；完整记录退出码与脱敏 stdout/stderr。
6. 无论 PASS/NO-GO，独立验证磁盘 evidence，并核对端口、PM2 socket/daemon、Nginx、systemd unit、work/control root cleanup。
7. Claude、Antigravity、Cursor 复核执行证据；更新正式进度，归档任务并提交本地审计记录。

禁止在任何失败后原地 retake、改 evidence、连接 production 或进入 D3–D6。
