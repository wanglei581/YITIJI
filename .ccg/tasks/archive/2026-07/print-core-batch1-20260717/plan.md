# Print Core Batch 1 Plan

1. 核对原型、现有页面、路由传参和 API 适配器；复用现有状态请求与业务流。
2. 为 Kiosk 添加原型 token 与可复用的打印页面结构样式，不改路由。
3. 对齐首页顶栏、设备状态、服务入口与底部导航，保持既有入口和登录态能力。
4. 逐页对齐上传枢纽、参数、材料检查、预览、确认、收银、进度、完成页，并只补计划指定的轮询与交互。
5. 按要求执行 TypeScript、lint、build；审查 diff 与原型截图；记录审查结果、更新正式进度文档；提交、推送和归档任务记录。
