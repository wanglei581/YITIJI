# 合并与部署边界

## 目标

- Squash 合并 PR #393 到 `main`。
- 仅部署包含手机号转移密码证明门禁的 API 预生产候选。
- 完成部署来源、PM2、health 与服务端代码存在性只读核对。

## 硬边界

- 不修改 Admin / Kiosk / Partner 静态包。
- 不执行数据库 migration 或数据写入。
- 不发送短信，不创建/验证转移 ticket，不修改真实账号或手机号。
- 不读取或输出管理员长期密码、JWT、OTP、ticket、数据库密码或短信密钥。
- 部署前必须建立可恢复回滚点；部署失败立即恢复。
