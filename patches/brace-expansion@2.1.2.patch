diff --git a/index.js b/index.js
index b8a519513f31c05e68eeb4d6929f8a151137f55c..67d827e2598fcda8a4f65838c364c7dafee2c8f3 100644
--- a/index.js
+++ b/index.js
@@ -2,6 +2,12 @@ var balanced = require('balanced-match');
 
 module.exports = expandTop;
 
+// Backport of the CVE-2026-14257 resource bounds from brace-expansion 5.0.8.
+// Keep the 2.x callable CommonJS export required by minimatch 5.
+var EXPANSION_MAX = 100000;
+var EXPANSION_MAX_LENGTH = 4000000;
+var EXPANSION_MAX_GROUPS = 256;
+
 var escSlash = '\0SLASH'+Math.random()+'\0';
 var escOpen = '\0OPEN'+Math.random()+'\0';
 var escClose = '\0CLOSE'+Math.random()+'\0';
@@ -66,7 +72,8 @@ function expandTop(str, options) {
     return [];
 
   options = options || {};
-  var max = options.max == null ? Infinity : options.max;
+  var max = options.max == null ? EXPANSION_MAX : options.max;
+  var maxLength = options.maxLength == null ? EXPANSION_MAX_LENGTH : options.maxLength;
 
   // I don't know why Bash 4.3 does this, but it does.
   // Anything starting with {} will have the first two bytes preserved
@@ -78,7 +85,13 @@ function expandTop(str, options) {
     str = '\\{\\}' + str.substr(2);
   }
 
-  return expand(escapeBraces(str), max, true).map(unescapeBraces);
+  var escaped = escapeBraces(str);
+  var groupCount = (escaped.match(/\{/g) || []).length;
+  if (groupCount > EXPANSION_MAX_GROUPS) {
+    throw new RangeError('Brace expansion group limit exceeded');
+  }
+
+  return expand(escaped, max, true, { remaining: maxLength }).map(unescapeBraces);
 }
 
 function embrace(str) {
@@ -95,7 +108,23 @@ function gte(i, y) {
   return i >= y;
 }
 
-function expand(str, max, isTop) {
+function pushBounded(target, value, budget) {
+  if (value.length > budget.remaining) {
+    budget.remaining = 0;
+    return false;
+  }
+  target.push(value);
+  budget.remaining -= value.length;
+  return true;
+}
+
+function boundedSingleton(value, budget) {
+  var result = [];
+  pushBounded(result, value, budget);
+  return result;
+}
+
+function expand(str, max, isTop, budget) {
   var expansions = [];
 
   // The `{a},b}` rewrite below restarts expansion on a rewritten string with
@@ -103,17 +132,18 @@ function expand(str, max, isTop) {
   // of non-expanding `{}` groups can't exhaust the call stack.
   for (;;) {
     const m = balanced('{', '}', str)
-    if (!m) return [str]
+    if (!m) return boundedSingleton(str, budget)
 
     // no need to expand pre, since it is guaranteed to be free of brace-sets
     const pre = m.pre
 
     if (/\$$/.test(m.pre)) {
       const post =
-        m.post.length ? expand(m.post, max, false) : ['']
+        m.post.length ? expand(m.post, max, false, budget) : ['']
       for (let k = 0; k < post.length && k < max; k++) {
         const expansion = pre + '{' + m.body + '}' + post[k]
-        expansions.push(expansion)
+        if (!pushBounded(expansions, expansion, budget))
+          return expansions
       }
       return expansions
     }
@@ -137,7 +167,7 @@ function expand(str, max, isTop) {
     // non-expanding `{}`, which is what made inputs like `a{},{},{}...` blow up
     // exponentially.
     const post =
-      m.post.length ? expand(m.post, max, false) : ['']
+      m.post.length ? expand(m.post, max, false, budget) : ['']
     var n;
     if (isSequence) {
       n = m.body.split(/\.\./);
@@ -145,11 +175,14 @@ function expand(str, max, isTop) {
       n = parseCommaParts(m.body);
       if (n.length === 1) {
         // x{{a,b}}y ==> x{a}y x{b}y
-        n = expand(n[0], max, false).map(embrace);
+        n = expand(n[0], max, false, budget).map(embrace);
         if (n.length === 1) {
-          return post.map(function(p) {
-            return m.pre + n[0] + p;
-          });
+          var combined = [];
+          for (var p = 0; p < post.length; p++) {
+            if (!pushBounded(combined, m.pre + n[0] + post[p], budget))
+              break;
+          }
+          return combined;
         }
       }
     }
@@ -194,21 +227,24 @@ function expand(str, max, isTop) {
             }
           }
         }
-        N.push(c);
+        if (!pushBounded(N, c, budget))
+          break;
       }
     } else {
       N = [];
 
       for (var j = 0; j < n.length; j++) {
-        N.push.apply(N, expand(n[j], max, false));
+        N.push.apply(N, expand(n[j], max, false, budget));
       }
     }
 
     for (var j = 0; j < N.length; j++) {
       for (var k = 0; k < post.length && expansions.length < max; k++) {
         var expansion = pre + N[j] + post[k];
-        if (!isTop || isSequence || expansion)
-          expansions.push(expansion);
+        if (!isTop || isSequence || expansion) {
+          if (!pushBounded(expansions, expansion, budget))
+            return expansions;
+        }
       }
     }
 
