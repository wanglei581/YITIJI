# 离线状态机探针（EV-006）

修订 8176c1ee2004 · 2026-09-02 · local（node stub，非真实渲染）

各实现方在 scratchpad 内用桩接出 `Page` / `wx` / `api` 跑页面状态机，探针本身未入库：

| 页面 | 断言数 | 覆盖场景 |
|---|---|---|
| fair-venue | 69（12 场景） | 三数据源各自失败/为空、平面图预览与 binderror 重试、搜索、展厅展开 |
| fair-visit-plan | 33 | 缺 fairId、无简历、GET 404、STALE_MODE、读取失败、命中已有结果不重复生成、四类失败分流、打印交接 URL |
| fair-companies | 8 场景 | 250/640 家、空列表、请求失败、缺参、详情 null、岗位为空、无 sourceUrl |

**限制**：只覆盖逻辑分支，不构成渲染证据。真实版式、折行、图片渲染一律未验。
