# 关键旅程仍未验证（EV-025）

**状态：PENDING。本文件的存在不代表任何验证已完成。**
**修订 `eff92ac9c063` · 2026-09-02 · 取代建包时的 `EV-009-real-backend-PENDING.md`**

（EV-009 已被生产只读探测占用，那条是 PASS；本条继续承载「仍然没验」的部分，
避免一个编号同时挂着「已验」和「未验」两种结论。）

## 一句话

批 1 的 12 个招聘会端点、批 2 的政策详情与保留期限、批 3 新增的三页，
**全部只对代码验证过**。响应字段是从 `services/api` 与 `packages/shared` 源码读出来的，
不是见过的真实响应。

建包后新增了 4 个页面和 1 个后端端点，**这个缺口一点没变**。功能变多不等于验证变强。

## J-01（行前计划 → 展位导览 → 参展企业 → 生成打印文件 → print-upload 报价 → 到机出纸）

- 打印交接刚从 `403 FILE_ACCESS_DENIED` 修通（透传 `printFileUrl`），
  **一次真实调用都没跑过**。
- 生产库里招聘会 0 条（EV-009），**就算今天拿到设备也走不到第一步**：
  没有招聘会可打开，展位导览 / 参会企业 / 活动资料 / AI 行前计划一条都进不去。
- 到机出纸依赖真实终端；`/terminals/public` 返回空数组，无可选终端。

## J-02（政策详情）/ J-03（我的文档保存期限）

- `GET /policies/:id` 本批新补，此前一直 404；真实响应未见过。
- 政策 0 条，政策条件自测今天打开必然是空态。
- 保留期限的 `consentVersion` 确认链路（R-06）未在真实登录态下走过。

## 建包后新增的四页，同样一步没验

| 页面 | 状态 |
|---|---|
| `pages/policy-check`（政策条件自测） | 端点契约已对生产核对过（EV-009），**页面渲染未验** |
| `pages/self-explore`（自我探索 · 五维雷达图） | 端点 `GET /resume/self-assessment/questions` 从未被真实请求；canvas 雷达图从未在真机上画出来过 |
| `pages/job-materials`（求职材料模板） | 全链路无 LLM，但生成与导出未真实调用过 |
| `pages/resume-build`（AI 简历从零生成） | 6 段表单 / 步骤条在 375px 的排布、textarea auto-height、受控输入是否跳光标，全部未验 |

canvas 雷达图这类**只有渲染出来才知道对不对**的东西，静态门禁一条都覆盖不到。

## 一律未测的面

- 认证：所有 `/me/*`、AI 生成、打印下单、文件访问
- 微信平台：合法域名未配置的话 `downloadFile` 资料预览必失败——**未查证**
- 性能：AI 长任务（`aiTimeout` 90s）真实耗时；公开只读端点实测 2–4s
- 可访问性、深色模式、小字号 / 大字号系统设置下的版式

## 谁能解掉

只有产品负责人能在微信开发者工具 / 真机上执行（清单见
`docs/acceptance/miniapp-fair-onsite-visual-spotcheck-2026-09-02.md`）。
**前置不是「有设备」，而是「生产库里有内容」**——没有内容时，
真机能验的只有壳、导航与空态。
