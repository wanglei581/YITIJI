schema_version: 1
project: "职易达 AI求职打印服务终端"
release_id: "kiosk-redesign-r1"
boundary: "local"
release_revision: "f0aa458e5c27220b089b1cf9941c4b4e3d79a156"  # origin/main 顶端（2026-09-07 订正；此前指向冻结分支 23464350f，该版本已不是工作所在位置）
generated_at: "2026-09-02T18:25:09+08:00"
overall_verdict: "NO_GO"
owners:
  accountable: "产品负责人（本仓 CLAUDE.md 指定的决策人）"
  product: "产品负责人"
  engineering: "TBD"
  qa: "TBD"
  operations: "TBD"
acceptance_authorities:
  business: "TBD"
  technical: "TBD"
  security: "TBD"
  release: "TBD"
environments:
  - name: "local"
    purpose: "development"
    url: "macOS 开发机；kiosk vite dev"
  - name: "production"
    purpose: "线上部署（百度云）"
    # 2026-09-08 15:2x 订正：按域名走。按 IP 探 /admin/ /partner/ 永远返回 kiosk，
    # 复验必须用域名或 curl --resolve，否则会得出「后台没部署」的错误结论。
    url: "https://zyidai.cn（IP 120.48.13.190）—— 2026-09-08 14:49 发布 b790767f6 成功；此刻落后 origin/main 3 个提交（#968 / #971 / #973）"
  - name: "windows-kiosk"
    purpose: "一体机真机（打印/扫描/Terminal Agent）"
    url: "PENDING —— 新 Windows 主机未验收"
gates:
  G0: {status: "PARTIAL", approved_by: "产品负责人", approved_at: "2026-05 及以前（定位、硬件、合规边界已封板，见 CLAUDE.md §1/§3/§15）"}
  G1: {status: "PARTIAL", approved_by: "产品负责人", approved_at: "2026-09-02（51 页原型 = 可执行的功能定义；107 路由与 471 端点契约齐备，逐页台账见 docs/reviews/wiring-ledger-2026-09-02.md）"}
  # 2026-09-07：G2 的 EV-003 已失效——取证于冻结分支 23464350f，此后 main 前进 200+ 提交、
  # 44 个文件经 8 个 PR 搬入，QX_MIGRATED_ROUTES 现为 4 条。须对 origin/main 重新取证后再改状态。
  G2: {status: "NO_GO", approved_by: "-", approved_at: "-"}
  G3: {status: "PENDING", approved_by: "TBD", approved_at: "TBD"}
  G4: {status: "PENDING", approved_by: "TBD", approved_at: "TBD"}
  G5: {status: "PENDING", approved_by: "TBD", approved_at: "TBD"}
  G6: {status: "PENDING", approved_by: "TBD", approved_at: "TBD"}

# ── 判定边界（不得合并成更强的整体结论）──────────────────────
verdict_by_boundary:
  # 2026-09-08 订正：「4/51」已被事实推翻。origin/main 的 KioskRoot.tsx 里
  # QX_MIGRATED_ROUTES 精确路由 = 25 条，另有 QX_MIGRATED_PREFIXES 与
  # QX_MIGRATED_EXACT_PATTERNS 两组模式再覆盖若干。
  # 注意「路由数 ≠ 51 页台账的页数」——一页可能对应多条路由，也可能一条路由承载合并后的多页
  # （如 /print/desk 由材料检查 + 预览两页合成）。所以这里**只记可直接点数的路由数**，
  # 不再写一个会持续腐坏的 N/51；页数以 docs/reviews/page-migration-inventory-2026-09-02.md
  # 对当时 main 现算为准。写死页数正是这份文档此前失真的原因。
  local_build: "PARTIAL —— 代码可构建、门禁通过；青序流光已迁 25 条精确路由（另有前缀/模式两组），打印链路 8 屏中 7 屏为新稿；其余页仍为旧版式"
  production:  "NO-GO —— 2026-09-09 07:47 已发布 930441bb3（deploy run 34325662631，依据 CI run 34279445389 name=CI/main/success；发布目标读「Resolve CI-verified target SHA」步骤日志，不看 deploy run 的 headSha）。复验：三端 200（zyidai.cn 内核 bundle index-DSgMkoxP.js、admin.zyidai.cn、partner.zyidai.cn 标题各自正确），/api/v1/health 回 status ok / db postgres / degraded []，GET 未知路径回「请求的内容不存在」（#975 的 4xx 文案修复确认在线）；青序流光首页已上线（生产 bundle 内含 qx-home 类，#932 引入）。仍判 NO-GO 的理由是 BL-06：岗位/招聘会/政策三个板块线上零内容"
  # 2026-09-08 产品负责人在「项目进展评估」会话中裁决现场形态为两期：
  # 一期 Windows 主机 + 外设（口径「打开这个网站，在本地电脑能完成打印操作就行」），
  # 二期才铺 27 寸一体机整机。因此本行必须分期表述 ——
  # 合成一句「一体机未验收」会把一期读成零验证，而一期首单实际已出纸（见 BL-03a）。
  device:      "分期判定 —— 一期（Windows 主机 + 打印机 + 扫码枪，外设形态）PARTIAL：真机打印首单已完成，剩余面板扫描与扫码枪付款码两项未实测，见 BL-03a；二期（27 寸一体机整机）NO-GO，见 BL-03b。domain-overlays 不变：物理设备不接受截图或绿色状态徽章作为 GO 证据"

# ── 硬阻塞（任一未清即不得宣称商用交付）──────────────────────
hard_blockers:
  - id: BL-01
    # 2026-09-08 15:2x 实测证伪：清掉浏览器隐私边界令牌后加载 https://zyidai.cn/jobs，
    # DOM 上是 data-kiosk-presentation="fusion-youth"、29 个 qx-* 类、文案为迁移后新版。
    # 取证注意：直接输网址会被 KioskPrivacyGuard 判为越过隐私边界而整页清场
    # （historyIndex === null 即 fail-closed），不清存储会误判成「页面挂了」。
    desc: "运行时新稿覆盖不全：已迁部分（打印链路、岗位、招聘会等）线上生效，其余页仍是旧版式。原描述「0/51 页」与「用户看到的仍是 V6/Gen1」已被实测推翻"
    owner: "工程"
    status: OPEN
  - id: BL-02
    desc: "生产实例落后 origin/main 且未做部署复验"
    owner: "运维"
    status: CLOSED
    closed_at: "2026-09-08"
    # 手动补发 deploy（workflow_dispatch + ci_run_id=34235749915）对 b790767f6 成功。
    # 复验：/api/v1/health 回 {"status":"ok","db":"postgres","degraded":[]}，
    # 前端 bundle 由 index-DRu6kBzl.js 变为 index-C6cJNZjg.js（证明产物确实换了，
    # 而不是只看 workflow 绿灯）。
    # ⚠️ 复验口径：`gh run view <deploy>` 的 headSha **不是**发布目标 ——
    # 那是 deploy 工作流自己 checkout 的 SHA。发布目标只能读
    # 「Resolve CI-verified target SHA」那一步的日志输出。此处曾据此误报过一次。
  - id: BL-06
    desc: "岗位 / 招聘会 / 政策三个业务板块线上零内容：2026-09-08 15:00 实测生产
      /api/v1/jobs、/api/v1/job-fairs、/api/v1/policies 三个 total 全为 0。
      代码全通、页面全在、空态诚实（无假数据、合规声明在位），所以这不是代码缺陷 ——
      是内容与授权来源问题。注意它不是「配置一开就有」：库里此前那 217 条岗位经审查拒绝发布
      （200 条已过期；来源机构名为「预生产…样本」「演示」；机构表内含 111、的撒的 等垃圾数据），
      按 CLAUDE.md §10 发布出去等于让求职者照着失效岗位去投递。需要真实数据源与授权。"
    owner: "产品负责人 / 运营"
    status: OPEN
  # BL-03 于 2026-09-09 拆为 a/b 两条。
  # 起因：原描述「Windows 一体机真机未验收」描述的是一期并不存在的设备 ——
  # 2026-09-08 产品负责人在「项目进展评估」会话中裁决现场形态为两期
  # （一期 Windows 主机 + 打印机 + 扫码枪 + 摄像头，二期才铺 27 寸一体机整机）。
  # 该裁决为该会话内的第一手记录；本文件由该会话落笔，出处即在此。
  # 合成一条会同时造成两个失真：把一期读成零验证（首单实际已出纸），
  # 并把二期的整机验收伪装成「快做完了」。
  - id: BL-03a
    desc: "一期（外设形态）真机验收未完成：Windows 主机 + 奔图打印机的打印首单已于 2026-08-07
      真实出纸（任务 ptask_kiosk_337d61d38b698d61 / 订单 cmsirshmc001clga8n1i2ind2，Agent 日志在档；
      本地 9527 API fail-closed 实测：无 Origin → 403 LOCAL_QR_ORIGIN_FORBIDDEN，
      带 Origin: https://zyidai.cn → 200 t_ksk_001 / KSK-001）。证据见
      docs/device/print-first-order-evidence-2026-08-07.md（#981 抢救进 main；此前该文档与两个 ID
      在 origin/main 上 grep 命中数均为 0，即证据存在但从未进过主干）。
      **剩余两项**：① 打印机面板扫描 —— Agent 日志反复报
      `scan-watcher: scanWatchFolder 未配置，跳过扫描监听`，需配置后按
      docs/device/peripheral-field-acceptance-2026-08.md 复验；
      ② 扫码枪 → 微信付款码支付 —— 未实测，需先确认商户「付款码支付」已开通，
      并探测 18 位 / 25–30 位码格式。摄像头按既有决策不接入。
      **边界**：该证据文档自述「不作整机商用通过结论」，本条不得据此宣称一期已通过；
      且首单发生于 2026-08-07，此后 main 前进数百提交，重新宣称前应在当前 main 上复打一单。"
    owner: "现场"
    status: OPEN
  - id: BL-03b
    desc: "二期（27 寸一体机整机）真机打印/扫描/Terminal Agent 未验收。整机到货后按全套真机验收重跑。
      Windows Agent 受控在线更新（PR #606）归属本条，不在一期上线承诺内，该 PR 保持挂起不关闭。
      注意 CLAUDE.md §9 的触控硬约束不因一期是本地电脑而放松（主按钮 ≥56px、可点击区 ≥48px、
      27 寸竖屏适配继续保留），二期复用同一套前端。"
    owner: "现场"
    status: OPEN
  - id: BL-04
    desc: "51 页原型目录 git 完全未跟踪（git ls-files = 0），无版本历史可回滚"
    owner: "工程"
    status: CLOSED
    closed_at: "2026-09-02"
    closed_by: "2d9f73c1b"
    note: "9.9MB 入库（53 HTML + 被引用资源），682MB 截图证据以目录级 .gitignore 排除；
      入库后从 worktree 位置重渲三页验证相对资源路径正常。本地备份
      docs/design/_backup-kiosk-redesign-20260902-1815 可删，不再是唯一保护。"
  - id: BL-05
    desc: "百度 OCR 密钥曾在聊天暴露，上线前须在控制台重建应用轮换"
    owner: "产品负责人"
    status: OPEN

# ── 进度口径 ────────────────────────────────────────────────
# 本包只承载闸门、证据与验收签署。项目进度的唯一信源仍是
# docs/progress/current-progress.md 与 docs/progress/next-tasks.md（CLAUDE.md §7）。
# 不在本包内维护第二份进度声明。
