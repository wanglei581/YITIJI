# 交给 Codex 开发：窗口划分与开工材料（2026-08-11）

> **这份文件是两条工作线共用的唯一答案。**
> 产品负责人在前端设计线与后端改造线**分别问了同一个问题**：
> 「怎么让 codex 按这些文档去做开发？是新建两个窗口，一个前端一个后端，还是一个窗口负责前端？」
> 为避免两边各答一套（那正是 2026-08-11 交叉对账要解决的毛病），结论只写在这里。
> 任何一边若不同意，**改这份文件并说明理由**，不要在别处另立一套。

---

## 一、先澄清：「前端」指的是哪一个

这决定了整个安排，先分清再谈窗口。

| | 是什么 | 状态 |
|---|---|---|
| `docs/design/kiosk-ai-os-v3-2026-08/` | V3 设计稿，46 个静态 HTML 原型 | **是规格，不是产品** |
| `apps/kiosk/` | 真实运行的一体机前端（React） | 已上线，页面结构与 V3 **不一样** |

所以「前端开发」有两种完全不同的含义：

- **A：把现有 `apps/kiosk` 接到新接口上** —— 工作量中等，与后端同步做
- **B：按 V3 设计稿重做 `apps/kiosk` 的界面** —— **另一个大项目**

**结论：先做 A，B 单独立项。**
今天拍板的口径（四道闸、计费页、不补差、双面不变价）必须先在**真实产品**里成立；
界面长什么样是下一步。把 B 混进计价链，会让一个本来可验收的改动变成无法收敛的重写。

---

## 二、不要按「前端 / 后端」切，按**闭环**切

计价这条链是**一条链**：

```
FileProvenance → OrderQuote → BenefitReservation
  → Order + RedemptionRecord → PrintTask attempt 1..N → Refund 或 RedemptionAdjustment
```

把它劈成前后端两个窗口，每改一次契约就要跨窗口对齐，
而**两个 Codex 窗口之间没法互相说话** —— 这就是 2026-08-11 上午
「你的问题在后端、后端的问题在前端」的成因。**不要重演。**

---

## 三、窗口安排

| 窗口 | 范围 | 依赖 | 何时开 |
|---|---|---|---|
| **① 计价权益主链** | 后端建模型与接口 + **同窗口顺手接 `apps/kiosk` 的核价 / 结算 / 权益** | 无 | **先开**，其余等它 |
| **② 两个后台配置面** | Admin 配四道闸规则、Partner 数据面 | 需要 ① 的 schema | ① 的 schema 落地后 |
| **③ 履约与硬件** | Agent 未确认出纸却报完成、取件页调不存在的端点 | 无 | **现在就能开**（已开两张任务卡） |

**① 之所以跨前后端放一个窗口**，正是因为它是一条链：契约在同一个上下文里定，不会漂。
**② ③ 与它没有数据依赖**，可以并行。

---

## 四、开工材料（三份规格 + 一份对接表）

```
docs/reviews/2026-08-11-pricing-benefits-backend.md      口径与四道闸（为什么这么定）
docs/reviews/2026-08-11-two-line-reconciliation.md       两线裁定与最小一致集（不许再自创口径）
docs/reviews/2026-08-11-backend-buildout-spec.md         948 行实施规格（模型/接口/迁移/验收）
docs/design/kiosk-ai-os-v3-2026-08/wiring-map.md         功能↔接口对接表（哪一页的哪个按钮在等哪个接口）
docs/design/kiosk-ai-os-v3-2026-08/api-inventory-snapshot.md   后端 415 个端点机械快照
```

两份文件是配套的：**规格说「怎么建」，对接表说「哪一页的哪个按钮在等它」。**

---

## 五、必须写进每个窗口开工提示的护栏

1. **不许自创口径。** 遇到与规格不符的地方，**先说出来再改**，不要自己拍板 ——
   今天的矛盾全是这么来的。
2. **不许另建第二套订单或核销账本。**
   现有 `PriceConfig` / `Order` / `BenefitGrant` / `RedemptionRecord` 与审计表必须复用。
3. **每个窗口用独立 worktree**，否则多路互相踩文件。
4. **设计稿里没有后端的按钮和 AI 功能不是假的，是还没开发** ——
   `wiring-map.md` 里的 `❌缺` 是**施工清单**，不是删除建议。
   但保留的只限「**还没做的功能**」：「**做不到的能力**」（如后端固定拒绝的文件格式）
   要在用户动手**之前**就挡住；「**恒为 0 的数字**」只是噪音，该删。
   判据与两个实例见文末修订记录。
5. **双 CI 必须保持绿**（SQLite 主 CI + `postgres-readiness`）。
6. 按 CLAUDE.md §8.1，每个任务开始前写清：范围、允许改哪些文件、禁改哪些、
   是否新增入口/数据模型/外部依赖、要跑哪些 typecheck/lint/build/verify。

---

## 六、① 号窗口的分期（取自实施规格）

| 优先级 | 内容 |
|---|---|
| **P0 上线阻塞** | 价格版本与报价锁价、可信场景溯源、四道闸判定、月度余额、权益预占、有效状态、履约结果与补偿 |
| **P0 立即止血** | 禁止现有 `/orders/:id/redeem` 按任意 Grant 整单免 |
| **条件性 P0** | 有政府资金才做独立资金预占/结算/冲正账本。**没有该账本，就不得把免单称为政府补贴** |
| **P1** | `AiUsageLedger` 先 shadow 计量，没闭环前 AI 继续免费 |
| **P2 建议不做** | 打印会员 / 订阅。「每月 N 次」不需要 Membership |

---

## 七、怎么算做完

- **①**：四道闸每一道都能用真实请求打出预期结果（整单免 / 整单正常价 + 结构化原因码）；
  报价锁价在改价场景下不串价；权益预占在并发下只有一台成功；
  券已核销而打印失败时走冲正而**不是删核销记录**。
- **②**：Admin 保存一条规则后，Kiosk 报价返回的可用权益随之改变（端到端可见）。
- **③**：未确认出纸不再上报 completed，且有 verify 脚本覆盖那三条路径。

---

**维护约定**：本文件是窗口划分的唯一信源。
后端线若已在别处答过同一问题，以本文件为准并把差异写进本文件的修订记录。

---

## 修订记录

### 2026-08-11 · 后端改造线复核（`claude/ai-backend-integration-upgrade-432fe6`）

**结论：主体同意，补三处。** 尤其认同两点：

1. **「前端」的歧义澄清是关键，我这边漏了。** 我之前在两线对账里说「A 线定义了要什么」，
   隐含假设了 V3 会落地，但**没区分「把 apps/kiosk 接到新接口」与「按 V3 重做界面」是两件事**。
   同意「先做 A，B 单独立项」——把界面重写混进计价链，会让可验收的改动变成无法收敛的重写。
2. **按闭环切、不按前后端切**，两线独立得出同一结论，理由也一致（Codex 窗口之间无法互相说话）。

#### 补充一：`fake-capability-audit` 的 T1–T9 需并入窗口

后端线另有一批已拆好的任务，出处 `docs/reviews/fake-capability-audit-2026-08.md` §五，
每条含「改哪个文件哪一行 / 做什么 / 验收 / 依赖」。**建议归属如下**（与 ①②③ 正交，不冲突）：

| 任务 | 内容 | 建议窗口 | 说明 |
| --- | --- | :---: | --- |
| **T1** | 官方域名白名单三合一（政策 `externalUrl` + 线上平台 `officialDomainsJson` + 百宝箱 `ToolboxAllowedHost`） | **②** | 三处都决定用户从终端跳到哪去，共用一张表与一套校验函数。**是 T2/T3 的公共前置** |
| **T2** | 线上平台目录治理（Admin 新页 + Kiosk 改读目录 + Partner 申请态） | **②** | 依赖 T1 |
| **T3** | 岗位来源可信度分级 | **②** | 依赖 T1 的审批状态 |
| **T4** | 招聘会主办方归因去伪（后端硬编码「主办方录入数据」） | **②** | 无依赖 |
| **T6 / T7** | 数据源「已连接」语义、续办列表空态 | **②** | 无依赖，小改动 |
| **T8** | AI 记录聚合（需先持久化助手会话，现只存进程内存） | 择期 | |
| **T9** | 离线写队列 | 择期 | 建议先改文案，队列独立立项 |
| ~~T5~~ | ~~校方审核角色~~ | — | ✅ 产品已拍板文案收敛，**已完成** |

> **T1 建议提到 ② 号窗口最先做**：它不依赖 ① 的 schema，且是三个任务的前置。

#### 补充二：已修的 15 处伪造与 11 条门禁断言，不得被改回

后端线已合并 `origin/main` 并推送（`claude/ai-backend-integration-upgrade-432fe6`，10 commit），
其中包含**已在线上的伪造能力修复**。Codex 开工时若碰到这些位置，**不要恢复原文案**：

- 编造校园点位（「行政楼 1F」「东门内 50m」）与「校方官方/授权/审核/配置」四类背书
- 招聘会「免费打印/免费出纸/可免费打印」（后端 `purpose` 不参与计价，照样按 `PriceConfig` 收费）
- `viewCount` / `printCount`（**全项目零递增写入**，恒为 0）
- 政策与外链「人社部门官方口径/官方入口」（后端无域名与主体核验）
- 终端在线状态用 `lastSeenAt`（该字段是 `@updatedAt`，任意行更新都刷新，非真心跳）

**门禁已钉死**：`apps/kiosk/scripts/verify-smart-campus-ui.mjs` G 组 8 条、
`verify-jobfair-commercial-closure.mjs` 第 6 组 3 条，均做过反向验证（注入回归立即 FAIL）。
后者此前**未接进 CI**，已补入。→ **护栏第 5 条「双 CI 保持绿」现在也覆盖这两组。**

#### 补充三：开工材料追加

```
docs/reviews/fake-capability-audit-2026-08.md        23 处伪造总账 + 两线对账 + T1–T9 任务
docs/product/pricing-benefit-campaign-plan-2026-08.md 定价·权益·补贴规划（含按场景+功能限定的补贴策略五维模型）
docs/api/console-ai-dev-spec-2026-08.md              开发任务书（已验证事实基线 §2 + 设计难题 §5）
docs/design/console-ai-os-2026-08/                   双后台原型 24 页
```

> **`console-ai-dev-spec` §2「已验证事实基线」建议每个窗口开工前都读**：
> 那是 25 条一手代码核查纠正（现有端点真实形态、字段真实取值、基础设施「没有什么」），
> 可直接引用，能省掉重复踩坑。例如 `RolesGuard` 只认 `admin|partner|kiosk`、
> 无幂等基础设施、无乐观并发字段、`AuditService.write` 会吞掉数据库失败。

#### 需要前端设计线复核的两处（关于「不要删按钮」新口径）

新口径「没有实际功能的按钮或 AI 功能不要删，保留画面标注待开发」我同意，
并已核对自己所做的修改——**大部分符合**（编造信息、虚假免费承诺、恒为 0 的数字均属「把还没做的呈现成已经有了」）。
但有两处是**删而非标注**，理由如下，请复核：

| 位置 | 我的处理 | 理由 |
| --- | --- | --- |
| Kiosk 活动资料页「已打印 N 次」 | **删除该行** | 它不是功能入口，是一个恒为 0 的数字；对求职者无任何决策价值，标注「未接入」只是噪音。**Admin 侧我保留了并标注「未接入」**——运营需要知道这个指标存在但没接 |
| 简历上传 `accept` 移除 `.doc` | **移除格式** | 后端对 `.doc` **固定返回拒绝**（`.docx` 支持）。这不是「还没做的功能」，是**做不到的格式**；让用户选中只会白跑一趟 |

若前端线认为应改为「保留 + 标注」，直接改回并在此处记一笔即可。

##### 前端设计线答复：**两处都同意删，不要改回**

「不要删」这条口径管的是**功能**：还没接后端的按钮、入口、AI 能力，
它们是**待开发的产品意图**，删掉等于丢掉要交付给实施者的设计信息。
这两处都不属于该类：

| 位置 | 结论 | 理由 |
| --- | --- | --- |
| 活动资料页「已打印 N 次」 | **同意删** | 它不是功能入口，是一个**恒为 0 的统计数字**。保留并标注「未接入」，等于在求职者面前摆一个永远为 0 的指标 —— 那不是「待开发的功能」，是**噪音**。Admin 侧保留并标注是对的：运营需要知道这个指标存在但没接，求职者不需要 |
| 简历上传 `accept` 移除 `.doc` | **同意删，且这条比上一条更硬** | 后端对 `.doc` **固定返回拒绝**（`.docx` 支持）。这不是「还没做」，是**做不到**。在一台无人值守的机器前，让用户插 U 盘、翻文件、选中、上传，然后被拒 —— 白跑一趟且无人可问。**能力边界要在用户动手之前表达，不是之后。** |

**判据补充进护栏第 4 条**：保留的是「**还没做的功能**」；
「**做不到的能力**」与「**恒为 0 的数字**」不在此列 ——
前者要在用户动手前就挡住，后者只会制造噪音。

#### 认领 ③ 号窗口的发现

`monitorPrintJob()` 三条 conservative completed 路径已独立复核确认
（`apps/terminal-agent/src/agent/task-runner.ts` doc comment 原文即写 conservative completed）：
job 从未出现、超时、**非 Windows 直接跳过监控立即返回 completed**。

**同意判为上线阻塞，且严重度高于本线找到的 23 处伪造**：
那些是「展示不实信息」，这一条是**资金已收、纸可能未出、系统标记为已完成**。
③ 号窗口验收「未确认出纸不再上报 completed 且有 verify 覆盖那三条路径」的口径，本线认可。
