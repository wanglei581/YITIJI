# 真实文件走查取证（批次 REAL-FILE-PRINT，2026-08-17）

分支 `claude/real-file-print`，基于 `origin/main@8188fcf13`。

自造 32 个真实素材，逐个走 kiosk 真实 HTTP 上传与体检链路（`POST /files/kiosk-upload` →
`materials/tasks` inspection / normalize_a4 / pii_scan → 预览页），在 **1080×1920**（一体机
竖屏分辨率）下截图。截图为原图等比缩到高 700px，未裁剪、未修改内容。

素材、走查脚本与完整结果 JSON 不入库（放在批次 scratchpad）；本目录只保留结论性截图。

| 图 | 说明 |
|---|---|
| 01 | 200×3000 极高图：预览区被撑到 **558×8370**（容器 560×8372），1920px 视口里只看得到顶部一小条。**未修**，见 PR 问题表。 |
| 02 | 3000×200 极宽图：`object-contain` 比例正确，但被居中在 2477px 高的框里，内容推到屏幕外。**未修**。 |
| 03 → 04 | 极宽图体检：修复前报「约 24 DPI，清晰度可能不足」（误报）→ 修复后「约 257 DPI」。 |
| 05 → 06 | 极高图体检：同上，24 DPI → 257 DPI。 |
| 07 → 08 | pdf-lib 压缩对象流 30 页 PDF：修复前「页数以实际打印为准」+ 逐个罗列 20 个页码 → 修复后「30 页」+「第 1-20 页…（本次仅检查前 20 页，共 30 页）」。 |
| 09 / 10 | 111 字符超长中文文件名：CSS 省略号截断，**不会截在半个汉字上**；但截的是尾部，扩展名与尾部区分信息丢失（见 PR 问题表，未修）。 |
| 11 → 12 | 16.93MB 超限：修复前「文件超出上限(15MB),大文件请用直传」（kiosk 前台没有「直传」入口）→ 修复后「文件 16.9MB，超过单份上限 15MB。请压缩后重试，或分成多份分别上传。」 |
