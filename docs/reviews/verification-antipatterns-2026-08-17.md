# 验证反模式清单（2026-08-17）

> **这份文件的存在理由**：2026-08-17 一天内，同一类错误发生了 **17 次**——
> 工具或检查器给出了看起来正常、实则错误的信号，而我们信了。
>
> 代价包括：一个 P0（首页招聘会恒为空）被自己的验证系统保护了很久；
> 五个正确的修复被门禁挡在外面；一次我把坏 YAML 合进 main，连累所有 PR 的 CI 转红。
>
> 归纳由 DeepSeek 与 antigravity 两个独立模型分别产出后合并。

---

## 一、七条根本模式

### 模式 1：工具假成功——退出码 0，但什么都没做

| 案例 | 表现 |
|---|---|
| `timeout 900 agy ...` | macOS 没有 `timeout` 命令，整条没执行，**退出码 0** |
| `agy -p --effort high "PROMPT"` | `-p` 把 `--effort` 当成了 prompt 值，它回答的是「什么是 --effort」 |
| `agy --sandbox` 无命令权限 | **不报错，凭知识编了个答案**——比报错更危险 |
| `codex exec` 认证过期 | 输出文件 **0 字节**，进程还挂着，退出码 0 |
| `bash -n f 2>&1 \| head -1 && echo "有效"` | `&&` 挂在 `head` 上，**无论如何都打印「有效」** |

**为什么难发现**：退出码 0 与「成功」在直觉上等价，而这些工具把失败写进了 stdout 或干脆静默。

**防呆**：
- 派出外部 CLI 后**先验产出**（文件大小、内容是否包含预期字段），再对外宣称它在跑。
- 管道里判断退出码要用 `PIPESTATUS`，不要让 `&&` 挂在管道末端的 `head`/`tail` 上。
- 正式派活前做一次**冒烟测试**：让它执行一条你已知答案的命令，对得上再派。

### 模式 2：检查了，但检查的是错的东西

| 案例 | 表现 |
|---|---|
| `command_timeout` 够不够 | 写成了判断它「存不存在」，存在就跳过 → 3m 超时没改到，扫描被截断两次 |
| basename 查引用 | `01-home.html` 同时命中两个不同目录的同名文件 → 0 引用误判成 8 处 |
| `git diff <ref> -- <path>` | 把**未跟踪文件静默报成「已删除」**，产出假的 `+0 -N` diffstat |
| 反向注入 `replace(..., 1)` | 子串出现多次只替换第一处 → 注入没生效，却当成「门禁没抓住」 |
| 提取 YAML 的 script 块做 `bash -n` | awk 缩进算错，新旧两份报同样的错 → **对照实验无效** |

**防呆**：
- 查文件引用**一律用仓库相对全路径**，或 `(^|/)filename$`，禁止裸 basename。
- 判断文件归属用 `git hash-object` 比对 blob，不要看 diffstat。
- 反向注入后**先确认注入真的生效**（残留计数为 0），再看门禁反应。
- 对照实验必须**先证明「坏的那份真的会红」**，否则实验无效。

### 模式 3：门禁本身不可靠（**代价最高**）

| 子类 | 案例 |
|---|---|
| **把 bug 钉死** | 门禁要求代码里必须存在一行有缺陷的判断（比对接口根本不下发的字段）→ 首页招聘会恒为空这个 P0 被保护了很久。**今天共发现 5 条同类** |
| 空转 | 断言 `/<KioskFeedbackDialog/`，组件改名成 `<KioskFeedbackDialogXX` 照样通过 |
| 报错不给证据 | 打印「违规文件：」后面是空的 |
| 汇总在明细前 | `N/M` 打在明细**前面**，日志一截尾就只剩个数字 |
| 空库恒绿 | 空数据库上通过、真实数据下才红 |
| 孤儿门禁 | 没注册进 package.json、CI 不引用 → 连元门禁的「342/348」都看不见它 |

**为什么最贵**（两个模型独立得出同一结论）：
其余模式造成的是**小时级急性阻塞**；这一条是**慢性剧毒**——
它不是漏过 bug，而是**主动杀死正确的修复**。工程师改对了，CI 却红，
于是怀疑自己并撤回改动，形成「自动化系统逼迫人写 bug」的闭环。
同时制造虚假安全感：「我们有 348 道门禁」。

**防呆**：
- 断言用**词界与闭合匹配**（`/<Component\b[^>]*>/`），或直接基于 AST。
- 门禁循环前加**非空底线断言**：`assert scanned.length > 0`。
- 新增/修改门禁必须做**先破后立**：注入缺陷 → 必须红 → 还原 → 回绿，三步都要在 PR 里留证。
- 报错格式统一为 `[FAIL] 规则 | 文件:行号 | 实际 vs 期望`；违规列表为空却返回失败，视为门禁自身缺陷。
- **严禁以「让门禁变绿」为目标改代码**。改断言前必须写明「为什么此断言先前是错的」。

### 模式 4：拿错误的快照当现状

| 案例 | 用的快照 | 真实现状在 |
|---|---|---|
| 「admin 发布按钮没修」 | 主仓工作区 | `origin/main`（早已修好） |
| 「API 构建挂了一整天」 | 主仓工作区 | `origin/main`（那模块只存在于工作区） |
| 「errorCode 到不了客户端」 | 生产 `dist` | `origin/main`（已加） |

本仓库有 **64 个 worktree**，主仓 checkout 常停在别的分支且带大量未提交改动。

**防呆**：任何「某文件/功能是否存在、是否已修复」的结论，动手前必须跑一条对 `origin/main` 的验证命令：

```bash
git ls-tree -r origin/main <path>     # 存在性
git show origin/main:<path>           # 内容
```

**不要**用 `ls`、不要读生产 `dist`（那是某个旧 SHA 构建的产物）。

### 模式 5：状态歧义——「没消息」不等于「进行中」

`gh pr checks` 在 PR 与 main 冲突时显示「no checks reported」，
与「排队中」**完全无法区分**——两个 PR 因此静静空转 25 分钟。

**防呆**：checks 为空时先查 `mergeStateStatus`，是 `DIRTY` 就先解冲突，不要当成 pending 等。

---

### 模式 6：测试把缺陷写成了期望值（**最隐蔽**）

前五条讲的是「验证没跑」「验证跑错了对象」「门禁本身不可靠」。这一条不同：
**门禁跑了、可靠、断言也没写错语法 —— 它只是钉在了错误的那一面。**

2026-09-08 实证。`apps/kiosk/tests/visual/kiosk-visible-actions-truth.spec.ts` 里写着：

```ts
expect(payload.factsConfirmedAt).toBeUndefined()
```

用例已经勾完事实核对墙、点了「确认导出」，页面也确实把时间戳传给了
`exportGeneratedResume(..., { factsConfirmedAt })`，而规格 `ResumeGenerateExportDto`
写的是**登录用户导出必填，缺失即 400**。真相是前端适配层按一条**已经过期的注释**
把该字段丢了，写用例的人照着当时观察到的行为，把这个缺陷固化成了期望值。

后果：**所有登录会员的「简历优化导出」100% 失败**（`400 RESUME_FACTS_NOT_CONFIRMED`），
匿名用户完全不受影响 —— 因为后端闸门对匿名早返回不校验。这条缺陷一路绿着活到今天。

**为什么它比「断言写漏了」严重一个量级**：写漏只是没抓到；
**把缺陷写成期望值，等于给缺陷发了一张永久通行证**，还让后来者以为那就是规格。
而且这类缺陷天然只打登录态，走查点一遍全绿：四观测量只记 method+path 不记状态码，
400 和 200 在日志里长得一模一样。

**两条判据**（产品负责人已采纳为全局检查项）：

1. 凡是断言「期望不存在」的（`toBeUndefined()` / `toHaveCount(0)` / `not.toContain()`），
   **要能说出为什么该不存在**。说不出来，就是照着行为写的。
2. **反向变异**：把被断言的行为改成「正确的样子」，如果测试转红，
   那这条断言钉的就是缺陷不是规格。
   （常规变异测试是「改坏了要红」，这里是**「改对了不该红」**。）

排查技巧：**先找同文件里做同一件事的兄弟方法**。本例 `exportResumeRecord` 一直是对的、
`exportGeneratedResume` 不对，两者相邻，一对一反证，比通读调用链快得多。

#### 「门禁在说谎」的四种形态（2026-09-08 一天内凑齐）

这四种**都让 CI 保持绿色，也都让后来者以为那就是规格**，但暴露办法各不相同：

| 形态 | 实例 | 怎么暴露 |
|---|---|---|
| **诱饵字符串**：声明了门禁看得见、用户看不见的东西 | 迁移线的 `PAYMENT_METHOD_LABELS` | 人工核对「门禁看见的」vs「用户看见的」 |
| **把缺陷写成期望值** | `expect(payload.factsConfirmedAt).toBeUndefined()` | 反向变异 |
| **断言所在的分支从不执行** | `waitReport` 把 `text=` 混进 CSS 选择器列表，Playwright 直接抛错；该分支只在解析慢时才走，mock 一步到位所以从没跑到 | 换真实 provider 走慢路径 |
| **分支执行了，但条件恒真** | ① 打印完成页断言弹层内的「满意度评分」为 0，而弹层默认关闭 ② `indexOf('ConversionBusyError')` 比顺序，而该名字在文件顶部 `import` 就出现 | **只能靠反向变异** |

**第三种和第四种要分清**：前者是**代码根本没跑到**，换环境（真实 provider、慢路径、真机）能暴露；
后者是**跑到了但恒真**，换多少环境都还是绿的，只有反向变异能戳破。

**而且两者会叠加**：把一条恒真断言改成可证伪的断言，**只有当它真的在 CI 里跑才产生价值** ——
否则只是把一个不会红的东西，换成另一个不会跑的东西。所以修完要多问一句：
**这条断言在哪个 job 里跑？那个 job 是必需检查吗？**
（正面例子：`factsConfirmedAt` 那条改完之后，正好被必需检查 `kiosk-browser-smoke` 的
`test:browser:truth` 拉着跑，所以它转绿这件事本身就是证据。）

第四种的第 ② 例还有一个细节值得记：**那条恒真断言是被变异测试逼出来的，不是靠人读代码发现的。**
`indexOf('ConversionBusyError')` 读起来完全合理 —— 谁都不会怀疑一个「检查错误类名出现顺序」的断言，
而它恒真的原因（那个名字在文件顶部 `import` 处就已经出现）**只在你真的去破坏它、看它不红时才会浮出来**。
**人读代码看不出恒真，这类断言只能靠变异发现。**

上表第四种的第 ② 例来自**当事人自己刚写的门禁**，不是历史遗留代码 ——
**任何人当场都可能写出恒真断言**，这不是代码质量问题，是缺少对照。

#### 变异测试不红时，先排除两种「假安心」

「我改坏了它却没红」有两种截然相反的原因，症状一模一样、结论完全相反：

| 失败模式 | 真实含义 | 对照办法 |
|---|---|---|
| **变异没打准** | 门禁可能是好的，我根本没碰到被测那一行 | **正向控制**：种一个哨兵字符串（如 `MUTATIONCONTROL9`），确认它进了构建产物 / 被测路径 |
| **变异不致病** | 门禁是好的，我这个「破坏」语义上仍然正确 | 先证明该变异确实破坏了行为，再看门禁反应 |

2026-09-08 两条都实际发生：一次是 `replace(old, new, 1)` 改到了同名断言的**第一处**
（目标在第二处），三次运行全无效而三次都绿；一次是把忙映射挪到通用兜底 `return` 之前，
语义仍正确，门禁没红，差点去改一条本来没问题的断言。

**所以：变异不红时，先答「变异真的作用到了被测那一行吗」和「这个变异真的把功能改坏了吗」，
两个都答『是』，才轮到怀疑断言。**

存量已扫过一次：kiosk 28 个 spec、312 条「断言不存在」里只有这 1 条属此类
（另 2 条「网撒得比规格宽」待产品判），清单见
[kiosk-absent-assertion-audit-2026-09-08.md](kiosk-absent-assertion-audit-2026-09-08.md)。
**是个别失误，不是系统性习惯** —— 但正因为稀有，下次再出现时更没人会怀疑它。

---

#### 变异的第三种「不红」：变异点不可达（2026-09-09）

既有的两种「假安心」之外，还有一种更容易被读反：**变异把构建弄挂了，测试根本没跑。**

为 #1003 那条 rAF 缺陷补行为用例时，反向变异前两发都是这个形态：

| 变异 | 结果 | 该怎么读 |
|---|---|---|
| 调用点退回直接 `rAF(...)` | **构建失败** | 调度函数变成未使用 —— **变异没走到断言** |
| 删掉 `setTimeout` 那一行 | **构建失败** | 常量变成未使用 —— 同上 |
| 兜底延时改成够不到测试窗口的值 | **用例失败** ✓ | 这一发才算数 |

**「构建失败」很容易被读成「变异有效」**——都是红的。
要验行为，**变异必须保持一切可编译**：只改值、不改结构，别让未使用符号把编译拦在断言之前。

#### 「门禁没在判」的三层（2026-09-09）

上面四种形态说的是**门禁跑了，但判据无效**。这三层是另一个轴：**断言可能完全正确，只是从未获得判据面**。
两者都表现为绿，但上面四种靠反向变异能戳破，这三层**变异也戳不破**——你破坏的对象根本没被看过。

| 层 | 形态 | 当日实例 | 静态可查？ |
|---|---|---|---|
| **1** | 文件从不在 CI 跑 | `apps/kiosk/tests/` 32 个 spec 有 9 个没有任何 config 的 `testMatch` 命中；`services/api/src/common/__tests__/launch-audit-p5.test.ts` 的 10 条安全用例（SSRF、审计强制写入、登录锁定绕过）只被 typecheck 编译，从未执行 | **能** |
| **2** | 跑了，但用例标题不匹配 project 的 `grep` → 贡献 0 条 | `cashier-qx.spec.ts` 挂进套件后仍贡献 0 条，且不报任何错；当时那句「18 passed」里没有一条来自它 | **能** |
| **3** | 跑了、标题也匹配，但断言全 skip | `back-exit.spec.ts` 冷开实测 **JUDGE 39 / SKIP 68**（60 条未渲染青序流光壳、3 条 →`/print/desk`、3 条 →`/print-scan/*`、1 条 →`/scan/start`、1 条 →`/resume/optimize`） | **不能** |

> **取证归属**：JUDGE/SKIP 的运行时数字由 bug 检查线在 `back-exit` 里打点实测；
> 跨端 skip 语句扫描（下方负面结果）由本线对 `origin/main` 复扫。
> 分开标注不是为署名，是为了将来有人质疑某个数时，找得到能为它辩护的人。

**第三层没法完全静态化**，这是两边分别证过的：

- 静态扫全仓只能查出「哪个文件有 skip 语句」，**跳多少条必须读运行时日志**。
- 反过来，为第三层加的「防退化成全跳过」下限断言也只能静态判部分跳过理由——
  `back-exit` 那两条 skip（冷开未渲染 qx 壳、冷开被 fail-closed 送走）**都是运行时才知道的**。

所以第三层的对策是**让门禁自己把判据面打印出来**（`[back-exit] JUDGE n · SKIP m`），
把跳过率变成可观测量，而不是让人去翻 Playwright 汇总倒推。
（倒推会错：当时按汇总倒推是「约 43 条」，打印出来是 **39** —— 差的 2 条是非路由的元用例。）

**跳过率高本身不是缺陷。** `back-exit` 那 68 条里 60 条是「尚未迁移的页不归它判」——
**它的判据面随迁移进度增长**。所以「back-exit 0 失败」的准确含义是
「**在它实际判到的那些路由里** 0 失败」，不是「所有路由都有返回出口」。
引用这类结论时必须带上判据面，否则会被读成后者。

**同理，不该为了降低跳过率去动被测对象**——不要为了让门禁有东西可判，就去补一个假的域根路由，
或伪造一个能渲染目标外壳的守卫态。跳过率高的正确解法是把页迁完，不是让门禁少跳。

##### 负面结果：第三层在本仓不是弥漫性问题

跨端扫描的结论，**写下来是为了让下一个人不必重扫**：

```
apps/kiosk/tests/visual/*.spec.ts   除 back-exit.spec.ts（2 处）外，全部 0 处显式 skip
apps/admin                          0 个文件
apps/partner                        0 个文件
services/api                        0 个文件
```

**全仓只有一个文件有显式 skip 语句。第三层集中在一条门禁上，不是系统性问题。**

##### 「没有判据面」的三种，区别在于计数看不看得出来

第三层再往下分，三种的**暴露难度递增**：

| 种类 | 形态 | 计数能看出来吗 |
|---|---|---|
| **判据面被跳过** | 有作用对象，但断言 skip 了 | **能** —— `SKIP 68` 摆在那里 |
| **压根没有作用对象** | 检查跑了，但它要查的东西在这个上下文里不存在 | **不能** —— 「0 条残留」和「0 个待查项」输出完全相同 |
| **匹配到了但不在渲染路径上** | 有对象、也匹配上了，匹配到的却是类型声明之类的死文本 | **不能** —— 计数是 1，看起来完全正常 |

第二种最容易骗人，因为它的输出和「真的清干净了」逐字相同。
迁移线在同步 `V6_SHELL_ROUTES` 时报告过这个形态：查残留数得 0、以为同步做对了，
看 diff 才发现那条路由本来就不在表里（该实例为当事人自述，此处取其形态，未逐条复核）。

**判据是：「检查通过」和「这个坑不适用」是两回事，而两者的输出一模一样。**
所以只要门禁不报「我查了哪几个对象」，这两种情况在外部看来永远无法区分。

**三种的共同解法只有一个：让门禁把「它检查了哪些对象」打印出来。**
`back-exit` 打 `JUDGE/SKIP`、`verify-content-trust-publish-gate` 打 partner 路由清单、
CI 排队问题把 run 的 job 数写进判据 —— 都是同一个动作。

> **只报结论不报作用面的门禁，绿的时候没人能判断它绿得有没有道理。**

##### 变体：匹配到了，但匹配到的不在渲染路径上

`verify:legal-doc-version` 断言源码里出现 `status: 'error'` 这串字符，
而它在**类型声明** `type LoadState = … | { status: 'error' }` 里也存在。
于是把错误文案整句删掉、把 `setState` 换成 `void 0`，**断言照样绿**。

这不是「跑了但 skip」，是「跑了、也匹配了，但匹配到的东西不在渲染路径上」。
**凡是读源码找字符串的断言都有这个风险**——它证明的是「这串字符存在于某处」，
不是「这个行为存在于运行时」。

##### 更上一层：门禁绿 ≠ 被防的事没发生

2026-09-08 专为「main CI 被连续取消」建的门禁 `verify:ci-main-runs-complete`，
**2026-09-09 全程绿着，而问题原样复发**。原因是它断言的是「配置写成了我相信的那个样子」，
而那个样子根本挡不住真正的成因（排队阶段被顶替，`cancel-in-progress` 管不到）。

**判断一条门禁有没有用，要看它断言的是配置形态还是结果。** 断言配置形态的门禁，
在「我们对机制的理解是错的」这种情况下会稳定地给出绿色——它忠实地验证了一个错误的假设。

#### 附：git 的「无冲突」不保证两边的改动都还在

2026-09-08 实测。两个 PR 都改了 `apps/kiosk/tests/interaction/sweep-harness.ts` 的不同区域，
rebase 时 git **没报冲突**。但「无冲突」只保证**文本能合**，不保证**语义都保留** ——
两处改动落在相邻区域时，完全可能一边被另一边整块替换吃掉，而 git 毫无察觉。

**收货动作**：两个 PR 改同一文件时，rebase 之后逐条 `grep` 确认**每一边的关键改动都还在**，
再跑一次该文件相关的最小验证。本次确认的是：A 侧的 `waitReport` 选择器修复仍在原位、
B 侧的 `scanForbiddenCopy` 接线仍有 6 处 + 2 处，并对改到的 5 个 `.ts/.mjs` 跑
`ts.createSourceFile().parseDiagnostics`（0 bad）、复跑单测（7 pass）。

相关口径：rebase 冲突时「两侧都保留」的自动策略**只对 `docs/progress/*.md` 这类纯追加文档安全**，
代码与 workflow 一律人工解。

---

### 模式 7：可信的机制 + 误读的现象 = 一个毫无破绽的错误结论

2026-09-08 实证，代价是**差点让人重启一台正在服务 1300 req/h 的生产服务器**。

当晚有人 SSH 连不上生产，观察到「ping 通、TCP 22/80/443 通、但 HTTP 与 SSH 应用层无响应」，
判断为「内核活、用户态推不动」，再联系到当天刚开的 soffice 转换（单次约 1G × 并发 2，
而机器总内存 3.8G），推出「内存耗尽 → 整机卡死」。

**这条链看起来完全站得住**：算术没错，soffice 确实吃内存，症状确实像卡死。
于是它被写进消息、被别的 lane 引用、被拿去当发布决策的依据。

事后取证，链上**没有一环成立**：

```
OOM 记录        无
系统重启        无（1 周 6 天未重启）
API 重启        无（13h uptime，重启计数未变）
soffice 残留    无
故障窗口 nginx  每小时 1250–1450 次访问，一次没断
一体机心跳      当日 1505 次，全部 200
```

**真因是发起排查的人自己被 `fail2ban` 封了**（`bantime=1h / maxretry=5`，一夜几十次 SSH
失败触发封禁，封禁后的重试继续累积失败，形成正反馈）。日志里 `Unban` 的时刻，
正是他以为「生产恢复」的时刻。

**要害在于：「ping 通、TCP 通、应用层无响应」既是整机卡死的特征，
也是「我的 IP 被防火墙 DROP」的特征 —— 两者症状一模一样。**
他只解释了其中一种，并且因为手上正好有一个可信的机制（soffice 吃内存），
那个解释显得格外可信。

**可信的机制只能说明「有可能」，不能说明「发生了」。**
机制越可信，越要单独去证实现象 —— 否则机制会替现象背书，而现象从来没被验过。

**推论**：不要用「这个解释很合理」当作证据。合理的解释可以有好几个，
而观测到的症状往往同时兼容它们。**要么找到能区分它们的证据，要么承认还不知道。**

---

## 二、验证防呆 7 问（动手前自问）

```
1. 我亲眼看到它失败过一次吗？
   → 现在故意把逻辑改坏，它会当场变红吗？

2. 这个 0 是做完了的 0，还是半路死掉的 0？
   → 报错被管道吞了吗？产物是不是 0 字节？命令在当前 OS 上真的存在吗？

3. 如果把输入全清空，它还会是绿的吗？
   → 空库、0 匹配、空列表会不会让循环直接跳过，产生假绿？

4. 我现在验证的，是我想验证的那份代码吗？
   → 工作区干净吗？dist 是刚打的吗？CI 是排队还是因冲突根本没跑？
   → 我验了内容，验了包住它的那一层吗？

5. 我是在测真实业务语义，还是在测一个名字/外壳？
   → 检查的是值和范围，还是仅仅查了它「存不存在」？

6. 这条断言是照着规格写的，还是照着我刚看到的行为写的？
   → 断言「期望不存在」时，说得出为什么该不存在吗？
   → 把行为改成「正确的样子」，这条测试会红吗？红了就是它钉着缺陷。
   → 变异了却没红：先确认变异打准了（哨兵字符串），也确认它真把功能改坏了。

7. 我宣布的这个故障，有没有独立于我自己的证据？
   → 「ping 通、TCP 通、应用层无响应」既是整机卡死的特征，
      也是**我的 IP 被防火墙 DROP** 的特征。两者症状一模一样。
   → 单一视角的「连不上」只能证明「我连不上」。宣布线上故障前，
      必须有一个独立于自己网络路径的证据：服务器侧日志 / 第三方探测 / 另一个出口。
```

> 第 4 条的最后一句来自 2026-08-17 的实际教训：
> 我用 `bash -n` 验了脚本、验了 base64 还原一致，**唯独没验包住它们的 YAML**——
> 而仓库里就有 `pnpm verify:repository-integrity` 专门干这件事，我没跑。
> **工具一直在，缺的是「什么时候必须跑」。**

---

## 三、改 workflow 的硬性要求

改动 `.github/workflows/**` 后，推之前必须本地跑：

```bash
pnpm verify:repository-integrity
```

它校验所有 workflow 的 YAML 语法。跳过它的代价（2026-08-17 实测）：
坏 YAML 进 main → GitHub 无法解析该文件 → 识别不出 `workflow_dispatch`（dispatch 一律 422）
→ 且 `Repository integrity gate` 转红 → **连带所有在跑的 PR 全部失败**。
