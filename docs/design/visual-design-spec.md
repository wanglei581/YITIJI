# 视觉设计规范标准

> 本文档为 AI求职打印服务终端的视觉设计标准，所有页面实现必须参照此规范。
> 适用于：kiosk（一体机前台）、admin（管理员后台）、partner（合作机构后台）
> 不适用于 AI SaaS 风格、渐变毛玻璃风格、营销页风格。
> 参考：Linear、Stripe Dashboard、Notion、Vercel Dashboard

---

## 一、核心原则

**设计目标：真实、克制、专业、可信赖**

| 原则 | 说明 |
|------|------|
| 真实 | 不追求炫技，每个元素都有存在的理由 |
| 克制 | 颜色、阴影、动画都要有节制地使用 |
| 专业 | 像运营级工具软件，不是展示型网站 |
| 可信赖 | 用户信任这个系统来处理他们的简历和打印 |

**禁止模式（必须避免）：**
- ❌ 紫色/蓝色渐变背景
- ❌ 卡片发光效果（box-shadow 彩色 glow）
- ❌ 毛玻璃效果（backdrop-blur 大面积使用）
- ❌ 霓虹色文字
- ❌ 每个模块都用主色强调
- ❌ emoji 作为 UI 图标
- ❌ 过度圆角（>16px）
- ❌ 浮动粒子/动态背景
- ❌ AI 机器人/对话气泡作为主要 UI 元素

---

## 二、颜色系统

### 2.1 主色（Primary / 科技蓝）

**主色只用于：关键操作按钮、选中态、当前态指示**

```
Primary 600   #2563eb  — 主要按钮背景、链接
Primary 700   #1d4ed8  — 主要按钮 hover
Primary 800   #1e40af  — 主要按钮 active
Primary 50    #eff6ff  — Primary 背景浅色（用于 badge 背景等）
```

### 2.2 语义色

```
Success   #16a34a  / 浅色背景 #f0fdf4  — 成功、在线、已完成
Warning   #ea580c  / 浅色背景 #fff7ed  — 警告、进行中、待处理
Error     #dc2626  / 浅色背景 #fef2f2  — 错误、失败、离线
Info      #2563eb  / 浅色背景 #eff6ff  — 信息提示
```

### 2.3 中性色（Neutral）

**所有边框、分隔线、文字、次要元素用中性色，不混用主色**

```
Gray 50    #f9fafb  — 页面背景（canvas）
Gray 100   #f3f4f6  — 卡片内部背景、次级区域
Gray 200   #e5e7eb  — 边框（border）、分隔线
Gray 300   #d1d5db  — 禁用态边框
Gray 400   #9ca3af  — 占位文字（placeholder）
Gray 500   #6b7280  — 次级文字
Gray 600   #4b5563  — 辅助文字
Gray 700   #374151  — 正文文字
Gray 800   #1f2937  — 标题文字
Gray 900   #111827  — 重要标题
```

### 2.4 三端配色差异

| 端 | 背景色 | 说明 |
|----|--------|------|
| Kiosk | Gray 50 (#f9fafb) | 一体机亮色背景，不开暗黑 |
| Admin | Gray 50 + 左侧菜单 Gray 900 | 亮色主内容 + 深色侧边栏 |
| Partner | Gray 50 | 同 Kiosk，亮色背景 |

**暗黑模式：Admin/Partner 支持暗黑，但必须使用专门的暗色 token，不允许直接用亮色反色。**

---

## 三、字体系统

### 3.1 字号阶梯

**21.5 寸触控屏 + 桌面后台，一体机阅读距离 50-80cm**

| 级别 | 字号 | 行高 | 字重 | 使用场景 |
|------|------|------|------|---------|
| text-xs | 12px | 1.5 | 400 | 标签文字、辅助说明 |
| text-sm | 14px | 1.5 | 400 | 次要信息、表格内容 |
| text-base | 16px | 1.6 | 400 | 正文（Kiosk 主要文字） |
| text-lg | 18px | 1.6 | 500 | 卡片标题、模块标题 |
| text-xl | 20px | 1.5 | 600 | 页面主标题 |
| text-2xl | 24px | 1.4 | 700 | 一级大标题 |
| text-3xl | 30px | 1.3 | 700 | Hero 标题（仅 Kiosk 首页用） |

**后台（Admin/Partner）text-base 可以降为 14px，因为是桌面操作距离。**

### 3.2 字重使用规范

| 字重 | 使用场景 |
|------|---------|
| 400（Regular） | 正文、说明文字 |
| 500（Medium） | 需要强调的正文、次级标题 |
| 600（Semibold） | 模块标题、侧栏菜单 |
| 700（Bold） | 页面标题、数字强调 |

### 3.3 字体族

```
font-sans: 'Inter', 'PingFang SC', 'Noto Sans SC', ui-sans-serif, system-ui, sans-serif
```

---

## 四、间距系统

**基于 4px 基准，复用 Tailwind spacing scale**

| Token | 实际值 | 使用场景 |
|-------|--------|---------|
| 1（4px） | 4px | 紧凑元素内部间距 |
| 2（8px） | 8px | 小间距、表单项内 |
| 3（12px） | 12px | 标准元素内间距 |
| 4（16px） | 16px | 卡片内间距、表单项间距 |
| 5（20px） | 20px | 模块内间距 |
| 6（24px） | 24px | 区块间距 |
| 8（32px） | 32px | 大区块间距 |
| 12（48px） | 48px | 大区块间距、触控安全区 |

**触控安全区：主要操作按钮四周不低于 16px 空隙，避免误触。**

---

## 五、圆角系统

| Token | 实际值 | 使用场景 |
|-------|--------|---------|
| radius-sm | 6px | Badge、标签、小型输入框 |
| radius-md | 8px | 按钮、标准卡片、标准输入框 |
| radius-lg | 12px | 大卡片、面板、弹窗背景 |
| radius-xl | 16px | 大弹窗、特殊卡片（仅必要时用） |
| radius-full | 9999px | 胶囊按钮、圆角头像 |

**原则：不要对所有元素都用同一个圆角值。按钮 8px、卡片 12px、badge 6px，分层清晰。**

---

## 六、阴影系统

**克制使用阴影，不要让页面看起来"浮在空中"**

| Token | 实际值 | 使用场景 |
|-------|--------|---------|
| shadow-none | none | 无阴影，不要用 |
| shadow-sm | 0 1px 2px 0 rgb(0 0 0 / 0.05) | 卡片默认阴影（白底+1px border+shadow-sm） |
| shadow-md | 0 4px 6px -1px rgb(0 0 0 / 0.1) | 弹窗、下拉菜单 |
| shadow-lg | 0 10px 15px -3px rgb(0 0 0 / 0.1) | Modal、浮层 |

**卡片标准样式：白底 + 1px Gray 200 边框 + shadow-sm**

---

## 七、触控规范（Kiosk 专用）

**21.5 寸触控屏，触控操作距离**

| 规范 | 最小值 | 推荐值 |
|------|--------|--------|
| 可点击区域（最小） | 48×48px | 56×56px |
| 主要操作按钮高度 | 56px | 56px |
| 次要操作按钮高度 | 48px | 48px |
| 底部 TabBar 高度 | 80px | 80px |
| 底部 TabBar 每个 Tab 宽度 | 屏幕宽÷3 | 自动 |
| 列表项高度 | 56px | 64px |
| 输入框高度 | 48px | 56px |
| 手指触控目标间距 | 8px | 16px |

**所有可交互元素的最小触控区域不小于 48×48px。**

---

## 八、三端布局规范

### 8.1 Kiosk（一体机前台）

```
┌──────────────────────────────────────┐
│  StatusBar (h-10)  标题 + 设备状态    │ ← 可选隐藏
├──────────────────────────────────────┤
│                                      │
│           Main Content               │ ← scrollable
│           (flex-1)                   │
│                                      │
├──────────────────────────────────────┤
│  TabBar (h-20) 首页│AI助手│我的       │ ← 固定底部
└──────────────────────────────────────┘
```

- 顶部状态栏：高度 40px，显示标题 + 设备状态（可选）
- 主内容区：垂直居中布局，避免顶部贴边
- 底部 TabBar：80px 高，三等分，图标+文字

### 8.2 Admin（管理员后台）

```
┌────────────┬──────────────────────────┐
│            │  Header (h-14)          │
│  Sidebar   │  面包屑 + 搜索 + 账号    │
│  (w-64)    ├──────────────────────────┤
│  深色背景   │                          │
│            │     Main Content         │
│  Logo      │     (flex-1)            │
│  菜单      │                          │
│            │                          │
└────────────┴──────────────────────────┘
```

- 侧边栏：256px 宽，深色背景（Gray 900）
- 内容区：浅色背景（Gray 50）
- Header：56px 高

### 8.3 Partner（合作机构后台）

```
与 Admin 布局相同，菜单项和权限范围不同。
配色、字体、间距与 Admin 保持一致。
```

---

## 九、组件视觉标准

### 9.1 按钮（Button）

| 类型 | 样式 | 使用场景 |
|------|------|---------|
| primary | 蓝色背景（Primary 600）+ 白字 | 主要操作（打印、确认、提交） |
| secondary | 灰色背景（Gray 100）+ 深色字 | 次要操作（取消、返回） |
| ghost | 透明背景 + hover Gray 100 | 辅助操作（更多、设置） |
| danger | 红色背景（Error）+ 白字 | 危险操作（删除、清空） |
| outline | 1px 边框 + 透明背景 | 切换态、文案按钮 |

**触控按钮：h-14（56px），文字 text-base（16px）**

### 9.2 卡片（Card）

**标准样式：**
- 背景：白色（white）
- 边框：1px Gray 200
- 阴影：shadow-sm
- 圆角：radius-lg（12px）
- 内边距：p-5（20px）或 p-6（24px）
- 标题：text-lg font-medium
- 描述：text-sm text-gray-500

### 9.3 输入框（Input）

- 高度：h-12（48px）触控友好
- 边框：1px Gray 300，focus 时 2px Primary 500
- 圆角：radius-md（8px）
- 错误态：边框红色 + 下方红色提示文字

### 9.4 表格（Table）

- 表头：bg-gray-50，文字 Gray 500，text-sm font-medium
- 行高：56px（触控友好）
- 斑马纹：奇数行 bg-white，偶数行 bg-gray-50
- 边框：底部 1px Gray 200

### 9.5 状态标签（StatusBadge）

| 状态 | 颜色 | 使用场景 |
|------|------|---------|
| 进行中 | Blue / bg-blue-50 text-blue-700 | 进行中、打印中 |
| 成功 | Green / bg-green-50 text-green-700 | 已完成、在线、正常 |
| 警告 | Orange / bg-orange-50 text-orange-700 | 待处理、缺纸、警告 |
| 失败 | Red / bg-red-50 text-red-700 | 失败、离线、错误 |
| 禁用 | Gray / bg-gray-100 text-gray-500 | 已取消、禁用 |

### 9.6 弹窗（Dialog/Drawer）

- 背景遮罩：black/50（rgba(0,0,0,0.5)）
- 弹窗背景：白色 radius-xl（16px）
- 阴影：shadow-lg
- 内边距：p-6（24px）
- 标题：text-xl font-semibold
- 关闭按钮：右上角 X，字号 20px

---

## 十、图标规范

**统一使用 lucide-react 线性图标，不使用 emoji**

| 规则 | 说明 |
|------|------|
| 图标风格 | 线性（stroke-based），24px 或 20px |
| 图标颜色 | 与文字同色，Gray 500 或 Gray 700 |
| 图标与文字间距 | 8px（gap-2） |
| 禁止 | emoji、带颜色的第三方图标库 |

---

## 十一、空态/加载/错误规范

### 11.1 空态（EmptyState）

- 图标：Gray 300，64px 或 48px
- 标题：text-lg font-medium Gray 800
- 描述：text-sm Gray 500，1-2 行
- 操作按钮：可选，放在描述下方

### 11.2 加载态（LoadingState）

- Spinner：Primary 500，32px
- 文字：text-sm Gray 500，"加载中…" / "处理中…"
- 全屏 LoadingOverlay：白色半透明遮罩 + 居中 Spinner + 文字

### 11.3 错误态（ErrorState）

- 图标：错误红色，64px 或 48px
- 标题：text-lg font-medium Gray 800，"出错了"
- 描述：text-sm Gray 500，具体错误信息
- 操作：重试按钮（primary）+ 可选返回按钮

---

## 十二、禁止模式清单

**以下做法在项目中严格禁止：**

| 禁止做法 | 替代方案 |
|---------|---------|
| 渐变背景 | 纯色白底/浅灰底 |
| 卡片发光（box-shadow 带颜色 glow） | 白色卡片 + 1px边框 + shadow-sm |
| 大面积毛玻璃 | 必要时单层 backdrop-blur，不做装饰用 |
| 霓虹色文字 | Gray 700 / Gray 800 |
| 每个模块都用主色强调 | 只有关键操作/当前态用主色 |
| emoji 作为 UI 图标 | lucide-react 线性图标 |
| 圆角大于 16px | 圆角统一按圆角系统 |
| 浮动粒子/动态背景 | 不要任何动态装饰 |
| AI 机器人形象作为主要 UI | 纯文字/线性图标 |

---

## 十三、验收标准

实现每个页面时自检：
- [ ] 没有使用任何渐变背景
- [ ] 没有发光卡片或霓虹色文字
- [ ] 按钮颜色符合主色规范
- [ ] 卡片有 1px 边框 + shadow-sm
- [ ] 触控区域不小于 48×48px
- [ ] 字体大小符合字号阶梯
- [ ] 状态标签配色正确
- [ ] 空态/加载/错误态符合规范
- [ ] 图标使用 lucide-react 线性图标
- [ ] 页面优先使用 `tokens.css` 已定义的语义 token，不直接散落 hex 颜色
- [ ] Admin/Partner 保持运营后台信息密度，不做营销页式大留白
- [ ] Kiosk 主操作路径不超过 3 步，主要按钮高度优先 56px

---

*本文档为 Mavis 编写，供项目所有开发人员参考。
如与 `packages/ui/src/styles/tokens.css` 冲突，先暂停实现并同步更新设计规范与 token 文件，避免出现两套视觉标准。*
