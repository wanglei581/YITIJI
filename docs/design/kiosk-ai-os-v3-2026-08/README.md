# Kiosk AI 操作系统 V3 · 设计基线说明书

> 状态：**基线已定（2026-08-08）**。视觉与构图基线 = `01-home-v5.html`「鲜彩玻璃」。
> 范围：27 寸 1080×1920 竖屏一体机前台。**本目录不改 `apps/kiosk/` 生产代码**，落地是第二阶段，需单独授权与文件预算。
> 本目录**不是**第二套项目标准（CLAUDE.md §8.1）。工程规模、目录职责、合规边界一律以既有正式文档为准。

## 零、与既有原型的关系

| 目录 | 角色 |
| --- | --- |
| `kiosk-proto-2026-07/`（78 个 html，即「75 屏」定稿） | **功能完整性真值**：只用来保证融合过程一个功能都不丢 |
| `kiosk-ai-orchestration-2026-08/` | **机制来源**：办理单 / 建议四要素 / 证据分级 / 权限红线 / 四态同版面 |
| `kiosk-visual-directions-2026-08/` `kiosk-ai-os-prototype-2026-08/` `kiosk-ai-os-style-study-2026-08/` `kiosk-ai-os-fusion-2026-08/` | 历史候选，不再作为输入 |
| **本目录** | 设计基线 + 26 页样张 + 覆盖矩阵 |

被本轮**推翻**的一条历史结论：`kiosk-ai-os-prototype-2026-08` 写的「没有渐变、毛玻璃、光晕」。本轮视觉禁令作废，机制与红线全部保留。

---

## 一、范式（不变）

**旧**：首页摆功能目录 → 用户自己判断问题归类 → 自己填参数 → 自己决定下一步。
**新**：用户带处境来 → 系统翻译成任务链 → AI 给**带理由的下一步** → 用户只做**确认**。

> **AI 承担"想"，用户承担"定"，系统与硬件承担"做"。AI 永不替用户按下不可逆的按钮。**

一体机特有约束（决定形态）：站立、可能排队、停留 3–8 分钟、手不干净、旁边有人。因此自由文本是兜底不是入口；每步只给一个主推荐；建议必须给理由；隐私默认超时清场。

## 二、核心对象：办理单（Errand）

```text
说清处境 / 直接选服务
  → 系统盘点已有材料与缺口
  → AI 给受控建议（动作 + 理由 + 代价 + 备选）
  → 用户确认关键动作
  → 服务端 / 支付 / 硬件真实门控
  → 结果进入本人资产、订单或第三方来源
```

字段：`errandId / intent / goalText / steps[] / assets[] / context / advice / status`。
意图为**封闭集 7 条**：`print / resume / fair / job / interview / policy / unsure`，不做开放域 NLU。
生命周期：90 秒无操作提示，120 秒清空全部本地缓存回待机。**登录门设在「沉淀」这一步，不设在入口。**

## 三、建议层（做不好整套就是花架子）

每条建议四要素齐全，缺一不发：**动作 / 理由 / 代价 / 备选**。

证据分级（**硬约束**）：

| 级别 | 来源 | 措辞权限 |
| --- | --- | --- |
| **E1** | 用户数据事实（页数、缺项、历史订单） | 可陈述、可祈使 |
| **E2** | 系统 / 设备 / 来源事实（彩色可用、今天有场次、截止日） | 可陈述、可祈使 |
| **E3** | 模型判断（表达优劣、匹配感受） | **必须标注「AI 判断，仅供参考」**，不得单独构成主建议 |

无 E1/E2 支撑不得用祈使句。本条只约束**建议层**；界面自身的操作指引（"先选一项服务"）不受此限。
每屏最多 1 条主建议 + 2 条备选。建议不可用时**整块换成「什么不可用、还能做什么」**，不显示空的 AI 壳子。

## 四、执行层权限矩阵（红线，不可协商）

| 动作 | AI 自动 | 需用户确认 | AI 永不执行 |
| --- | :---: | :---: | :---: |
| 识别文件类型 / 页数 / 是否简历 | ✅ | | |
| 预填打印参数（标注"已预设，可改"） | ✅ | | |
| 材料完整性预检 / 简历诊断打分 | ✅ | | |
| 生成简历新版本 | | ✅ 点「采纳」 | |
| **覆盖 / 删除原简历** | | | ❌ 只生成新版本 |
| **发起支付 / 出纸 / 删除文件** | | ✅ | |
| **跳转第三方来源** | | ✅ 离站确认页 | |
| **平台内投递 / 代收简历** | | | ❌ 合规红线 |

## 五、四态与失败路径

每页必须成立四态，且**四态同一套版面**：`默认 / 首次使用（无上下文）/ AI 不可用（降级）/ 设备离线`。
不因状态重排页面、不换栅格、不抻长卡片；只换该位置的话与主按钮。

| 故障 | 降级 | 绝不能做 |
| --- | --- | --- |
| AI 不可用 | 退目录模式，业务不阻断 | 空建议卡 / 假建议 |
| OCR 置信度低 | 标注需人工复核，**不送 LLM** | 用低置信文本出结论 |
| 打印机离线 / 缺纸 | 改为"转存我的文档"或换机取件 | 显示"设备正常" |
| 支付成功但打印失败 | 待打印订单 + 取件码，可换机复打 | 静默失败 |
| 会话超时 | 凭手机号找回未完成办理单 | 丢弃已付费任务 |
| 不满意 AI 结果 | 版本列表可回退 | 覆盖原件 |

---

## 六、视觉与构图基线（本轮新定）

### 6.1 配色

默认主题 **`warm` 暖玉纸**：象牙纸底 + 深青玉主色 + 铜金辅色。
`styles/themes.css` 提供 9 套主题（warm / honey / frost / amber / jade / cobalt / neon / pine / ink），任何页面加 `data-theme` 即整套切换。

**两套色永不混用**：

- **状态语义色**：青玉＝本机服务与已验证 / 钴蓝＝第三方与官方来源 / 琥珀＝待核实 / 朱砂＝需本人确认的不可逆动作。只出现在状态点、标签、按钮。
- **服务身份色**（`--cat-*`）：松青 打印 / 铜金 简历 / 靛蓝 岗位 / 赤陶 招聘会 / 湖青 面试 / 石板蓝 政策。只出现在服务入口色块与图标底。

**刻意不设紫色 / 靛紫**——紫蓝渐变是当前 AI 产品的通用皮，用了就落进"AI 味"。

### 6.2 构图三条

1. **纵深靠重叠与投影**，不靠更多描边：玻璃指令胶囊用负边距跨在色域版头底边上。
2. **玻璃只浮在大图或色域之上**，纯色底不用玻璃（会显脏）。
3. **比例由任务决定**，不由组件默认值决定：入口刻意不等宽，文件类页面预览占 55–65%。

### 6.3 竖屏分区（全站强制）

```text
  0 ─  100px   顶栏：品牌 / 设备 / 时间        只看不摸
100 ─ 1688px   内容主体（唯一滚动容器）        视线自然落点
1688─ 1812px   ActionBar 常驻，主按钮恒定右下  手的舒适落点
1812─ 1920px   底部导航：首页 / AI 顾问 / 我的
```

主按钮 ≥64px 高，可点击目标 ≥48px，ActionBar 不参与滚动。

### 6.4 动效纪律

每个效果必须能说出它在表达什么，否则不做：

| 效果 | 含义 |
| --- | --- |
| 数据流连线 | 顾问 → 你的指令 → 排出的路径是一条链 |
| 路径流光 / 节点光环 | 办理方向；当前这步是活的 |
| 玻璃反光跟手 / 按压微倾斜 | 触点反馈（一体机没有鼠标） |
| 计数点阵 | 真实数量（12 个点 = 12 家来源） |
| 系统脉搏线 | 终端每 5 秒上报一次 |
| 处理日志 | AI 此刻在读什么、核对什么（不是转圈圈） |
| 启动序列 | 系统开机，不是网页加载 |

**禁用**：紫蓝渐变 / 发光星星 / 魔法棒图标；**数字滚动**（中间值会被当成真实读数，等于伪造数据）；语音波形（本机没有语音下单能力）；粒子雨星空（纯装饰且吃 GPU）。

只动 `transform / opacity / filter`。`prefers-reduced-motion` 全部退化；`html.lowgpu` 关闭模糊与色域动画。

### 6.5 文件与令牌

```text
styles/tokens.css      设计令牌唯一来源（色/圆角/间距/字号/动效/分区）
styles/themes.css      9 套配色主题
styles/base.css        舞台 / 极光 / 颗粒 / 顶栏 / ActionBar / 导航
styles/components.css  公共组件（panel / btn / chip / ev / dot / tile / advisor / kv / notice…）
styles/vivid.css       V5 构图层（色域版头 / 玻璃胶囊 / 身份色卡 / 横向路径 / 仪表）
styles/fx.css fx2.css  动效层
scripts/sprite.js      图标库（24×24 / stroke 1.75 / 内联 symbol 注入）
scripts/stage.js       工作台外壳 + 四态切换 + 自查
scripts/fx.js fx2.js   动效行为
```

**token 化强制**：色值、圆角、间距、字号、动效时长一律走变量，禁止硬编码。同名组件全站一种长相。
**零外部依赖**：无 CDN / 远程字体 / 远程图片，断网可渲染。

---

## 七、78 屏 → 26 页 覆盖矩阵

融合原则：**同一件事的多个步骤页合成一个多阶段工作台**；能力一个不丢，入口不重复。

| 新页 | 页面 | 承接原型编号 | 融合说明 |
| --- | --- | --- | --- |
| **P01** | 首页 · 意图台 | 01 | 六个定版入口 + 办理单编排 + 后台开关专区入口 |
| **P02** | 待机唤醒屏 | 57 | 轮播 + 触摸唤醒 + 离线缓存 |
| **P03** | 身份门 | 15, 63 | 扫码 + 手机号 + 游客；弹层形态，不做独立页跳转 |
| **P04** | 系统态 | 58, 59, 60, 61, 77 | 帮助 / 法务 / 会话超时 / 断网异常 / 会话恢复，一页多形态 |
| **P05** | 手机接力 | 62, 63 | 390×844；只承接当前一体机会话，不建第二套导航 |
| **P06** | 打印工作台 | 02, 03, 04, 31, 32, 33, 64, 65, 77, +取件认领 | 上传→体检→预览→参数→确认→收银→出纸→取件，八阶段一页 |
| **P07** | 扫描工作台 | 34, 35, 36, 37 | 类型→指引→等待→结果，四阶段一页 |
| **P08** | 文件加工台 | 66, 67, 68 | 图片转 PDF / 签章 / 能力说明（含"即将上线"诚实标注） |
| **P09** | 简历工作台 | 05, 06, 07, 26, 27, 28 | 来源→解析→诊断→优化对比→版本→导出打印 |
| **P10** | 简历访谈式生成 | 25, 26 | 表单改为访谈式；预览与导出复用 P09 |
| **P11** | 岗位匹配对比 | 55 | 三档参考（禁百分比）+ 逐项证据对照 + 改写建议 |
| **P12** | 材料工厂 | 29, 30 | 模板库 + 求职信 / 自我介绍 / 清单 / 证书生成 |
| **P13** | 岗位情报台 | 08, 78 | 列表 + 筛选 + 线上线下双轨 + AI 收敛 |
| **P14** | 岗位详情 | 09 | 来源机构 / 同步时间 / 外部 ID / 扫码投递 / 去来源平台 |
| **P15** | 企业导览 | 53, 54 | 找企业 + 企业详情（指标受 Admin 开关控制） |
| **P16** | 线下机构 | 74, 75 | 机构列表 + 线下岗位详情；仅信息展示与到店指引 |
| **P17** | 招聘会作战台 | 10, 11, 43, 44, 45, 46, 47, 48, 49 | 场次→企业→展位导览→AI 准备单→资料打印→签到→现场数据 |
| **P18** | 校园招聘专区 | 50 | 校招双选会沉浸浏览 |
| **P19** | 智慧校园 | 51, 52, 69, 70 | 迎新 + 自助服务 + 未开放锁定态（绝不展示假数据） |
| **P20** | 面试训练舱 | 38, 39, 40, 41, 42 | 设置→逐题→报告→历史→技巧 |
| **P21** | 政策服务 | 12 | 三类政策 + 材料清单打印 |
| **P22** | 职业规划 | 56, 76 | 现状 / 方向建议 / 行动项 / 打印 |
| **P23** | 我的 · 资产中心 | 14, 16–23, 71 | 简历 / 文档 / 订单 / AI 记录 / 收藏 / 权益 / 通知 / 设置 / 浏览跳转记录，分区 + 抽屉 |
| **P24** | 权益活动 | 24, 72 | 活动列表 + 详情领取 |
| **P25** | 顾问驾驶舱 | 13, 73 | 小青 + 文字对话 + 语音咨询态 + 九能力入口 |
| **P26** | 顾问作业面 | 九能力 | A 问答型 / B 填槽型 / C 比对型，三态同页 |

### 反向自查：01–78 逐号归属

```text
01→P01  02→P06  03→P06  04→P06  05→P09  06→P09  07→P09  08→P13
09→P14  10→P17  11→P17  12→P21  13→P25  14→P23  15→P03  16→P23
17→P23  18→P23  19→P23  20→P23  21→P23  22→P23  23→P23  24→P24
25→P10  26→P09/P10  27→P09  28→P09  29→P12  30→P12  31→P06  32→P06
33→P06  34→P07  35→P07  36→P07  37→P07  38→P20  39→P20  40→P20
41→P20  42→P20  43→P17  44→P17  45→P17  46→P17  47→P17  48→P17
49→P17  50→P18  51→P19  52→P19  53→P15  54→P15  55→P11  56→P22
57→P02  58→P04  59→P04  60→P04  61→P04  62→P05  63→P03/P05  64→P06
65→P06  66→P08  67→P08  68→P08  69→P19  70→P19  71→P23  72→P24
73→P25  74→P16  75→P16  76→P22  77→P04/P06  78→P13
```

**78 个原型编号全部有归属，零丢失。** 新增能力（非原型带来的）只有一处：`/print/pickup-claim` 扫码取件认领，归 P06，对应真实已存在的生产路由。

### 付费 / 硬件 / 后台依赖（只列有依赖的页）

| 页 | 付费点 | 硬件依赖 | 后台依赖 |
| --- | --- | --- | --- |
| P06 打印工作台 | 有（收银台，金额以现场公示价为准） | 打印机、U 盘、扫码器 | 价格配置、打印任务、Agent |
| P07 扫描工作台 | 无（本阶段） | 扫描仪 / ADF | 扫描会话 |
| P08 文件加工台 | 视能力 | 打印机（输出时） | 转换 / 签章服务、能力开关 |
| P09–P12 简历与材料 | AI 次数 / 权益 | 打印机（输出时） | AI Provider、模板库 |
| P17 招聘会作战台 | 打印资料时 | 打印机 | JobFair API、导览图、准备单 |
| P19 智慧校园 | 无 | 无 | 校方开关（未开启不渲染） |
| P20 面试训练舱 | AI 次数 | 麦克风（语音可选） | 面试服务、语音能力探测 |
| P23 我的 | 无 | 无 | 会员资产、文件 TTL |

---

## 八、合规红线（与 CLAUDE.md / docs/compliance 一致）

- 按钮白名单：查看岗位 / 去来源平台投递 / 扫码投递 / 查看招聘会 / 去来源平台预约 / 扫码预约。
  **禁止**一键投递 / 立即投递 / 平台投递 / 收简历 / 候选人管理。AI 生成的建议文案同样过白名单校验。
- 岗位匹配只给三档参考（较高 / 中等 / 偏低），**禁止百分比、禁止录用概率**。
- 岗位 / 招聘会 / 企业**详情页**必须展示来源机构、同步时间、外部 ID；列表卡至少来源机构 + 同步时间。
- 金额一律「以现场公示价为准」；不硬编码打印机型号（用 `printerName`）。
- 无真实接口 / 硬件状态 / 保存结果时，不得展示已完成、已保存、已投递、已打印、设备正常。

## 九、验收口径（每页交付前必须过）

1. 四态可切换且都成立，**四态同一套版面**（编排带高度、卡片高度、卡内空白基本一致）。
2. 1080×1920 下：`主体纵向溢出 = 0`、`横向溢出 = 0`、`触控不足 48px = 0`、`违禁文案 = 0`。
   工作台内置自查：打开页面看 console，或调 `window.v3Audit()`。
   多阶段页要跑**全组合**（阶段 × 四态），不是只看默认态。
   违禁文案检查**允许否定式声明**：「本机不代收简历」「不预测录用概率」这类合规披露必须能写，
   否则页面会被逼着回避这些词、反而说不清边界；肯定式用法仍然必抓（已做反向验证）。
3. 主按钮 ≥64px、可点击 ≥48px；ActionBar 主按钮恒定右下。
4. 每条 S1 建议四要素齐全，理由标明证据级别。
5. 零外部依赖，断网可渲染；`prefers-reduced-motion` 下动效退化。
6. 文件类页面预览占视觉主体 55–65%，多页可翻页并显示「第 X / 共 Y 页」。

## 十、进度

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| 0 | 设计基线 + 说明书 + 覆盖矩阵 + P01 首页 | ✅ 已交付 |
| 1 | P06 打印工作台 · P07 扫描工作台 · P08 文件加工台 | ✅ 已交付 |
| 2 | P09 ✅ · P10 ✅ · P11 ✅ · P12 ✅ 材料工厂 | ✅ 已交付 |
| 3 | P13–P16 岗位企业 | 进行中 |
| 4 | P17–P19 招聘会校园 | 待做 |
| 5 | P20–P22 面试政策规划 | 待做 |
| 6 | P23 我的 · P24 权益 · P25/P26 顾问 | 待做 |
| 7 | P02–P05 系统态与接力 + 终审收口 | 待做 |

每批：设计 → 浏览器 1080×1920 四态实测 → Codex 只读联审 → 修 → 截图交付。

**本机 Codex 调用注意两点（2026-08-08 踩过，后续别再踩）**：

1. **必须重定向 stdin**：`codex exec ... < /dev/null`。否则它会一直等 stdin 关闭而**静默挂住**，
   输出 0 字节，看起来像在思考——本轮因此空跑 56 分钟。
2. **必须用真实二进制路径，不要走 `~/.local/bin/codex` 软链接**。该软链接指向
   `/Applications/ChatGPT.app/Contents/Resources/codex`（codex 随 ChatGPT 桌面端分发），
   而 codex 按 argv[0] 所在目录寻找辅助程序 `codex-code-mode-host`，走软链接时在 `~/.local/bin/`
   找不到，报 `failed to spawn code-mode host`，后果是**它读不到任何文件**、结论没有证据；
   `--disable code_mode` 无效。正确调用（无需重装）：

   ```bash
   /Applications/ChatGPT.app/Contents/Resources/codex exec -s read-only --skip-git-repo-check "<题目>" < /dev/null
   ```

## 十一、怎么看

```bash
python3 -m http.server 5293 --directory docs/design/kiosk-ai-os-v3-2026-08
```

- `index.html` — 26 页总览
- `compare.html` — 首页三版构图对比（甲 玻璃卡片 / 乙 印务台 / 丙 鲜彩玻璃 ← 基线）
- `themes.html` — 9 套配色对照
- 单页参数：`?theme=warm|honey|frost|…`、`?state=first|ai-down|device-off`、`?capture=1`（隐藏外壳按原尺寸截图）

## 明确不在本轮范围

不改 `apps/kiosk/` 生产代码；不实现 Admin / Partner 配置项（只在矩阵标注缺口）；不做物理目录迁移。
