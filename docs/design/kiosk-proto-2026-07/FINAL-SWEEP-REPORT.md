# 终轮 QA 联审收尾报告(75 屏 Kiosk 原型)

> 状态:四阶段全部完成。本文件为断点续传快照 + 最终结论,2026-07-17。
> 所有修改只写原型正本(本目录);worktree 下 `.proto-review/` 快照已删除;未动真实源码与 index.html。

## 阶段1:3 处已确认出界 → 已修复

| 屏 | 问题 | 修法 |
|---|---|---|
| 08-jobs-list | pager 及子元素出界 27 个(内容总高超限) | 压缩行高/间距/chip 字号;修复「来源」筛选行 988>984px 折行;后又按屏幕填充度回调 padding |
| 62-phone-upload | p-fill 不定长进度动画 rect 扫出手机舞台 | 改为 background-position 扫动(元素恒 inset:0),含 prefers-reduced-motion 降级 |
| 63-qr-login-mobile | m-notice 出界(844px 超限) | 压缩 stage padding/gap、终端卡 icon 与间距、m-safe 字号 |

## 阶段2:全 75 屏程序化体检 → 全绿

检测项:①stage.scrollHeight>1930 ②scrollWidth>1090 ③元素越 stage 边界>8px ④无 #stage(手机屏跳过尺寸检查改测自身溢出)。
沙箱 iframe(sandbox=allow-same-origin 禁脚本)分批加载,每批前校验 index 75 链接。

- 初扫新发现并修复:**10-fairs-list**(出界 124px)、**53-companies**(出界 140px)。
- 根因发现并修入 shared.css:`.row-item .r-main b { display:block; font-size:26px }` 污染元信息 chip 内的 `b`(所有列表屏 chip 被撑成 45px 高、26px 粗体)。新增 `.row-item .r-main .chip b { display:inline; font-size:inherit }`。
- 追加体检维度(超出任务书要求):**卡内 overflow:hidden 裁切检测** + **按钮文本折行检测(Range rects)**,新发现并修复:
  - 01-home:分组卡内容被裁最多 181px(岗位卡「找企业/岗位大师」整行不可见)→ 压缩欢迎区/磁贴/间距,全部内容入卡
  - 06-resume-diagnosis:总评卡裁 138px(雷达图/说明被切)→ 缩雷达 SVG、压缩分数块;底部三按钮折行 → nowrap+缩 padding+文案「目标岗位匹配参考(仅供参考)」→「岗位匹配参考」
  - 38-interview-setup:content 溢出 170px(Codex 亦命中)→ 全面压缩选项卡
  - 11/45/48/53/54/74/75:卡内裁切 13~60px → 逐屏压缩 padding/gap
  - 57 `.frame`(装饰出血)与 44 `.r-desc`(line-clamp 省略)判定为有意设计,不改
  - shared.css:`.back-btn` 加 `flex:none; white-space:nowrap`(05/06/07/24/25 等屏「返回」被挤成竖排单字);`.actionbar .btn` 加 nowrap
  - 逐屏善后 nowrap 引入的挤压:11(actionbar 缩 padding)、12(政策办理双按钮)、24/42/55(pagehead aside 按钮)、29/30/45(卡内按钮)
- 5 屏截图人工核查(01/08/32/40/74)+ 自查追加:10/53(pagehead 拥挤折行→53 返回按钮缩短+badge nowrap)、13/39/46/64(46 底部按钮折行→nowrap+文案缩短)。
- **终扫结论:75/75 屏 出界 0、纵横溢出 0、卡内裁切 0、按钮折行 0。**

## 阶段3:Codex(GPT-5.6-sol)三段走查 → 逐条核实

三段各一次 codeagent-wrapper(codex/reviewer);段1 首跑挂起 15 分钟无产出,已 kill 重跑成功。

### 段1(01–25):38 条 → 采纳 9 / 拒绝 29
采纳并修复:
1. 01 首页四卡内容被 overflow:hidden 裁切(实测最大 181px)→ 布局压缩,全量入卡
2. 06 总评卡裁切 138px → 已修
3. 06 底部按钮折行 → 已修(nowrap+文案)
4. 05 「云端上传·云盘同步目录」为虚构能力 → 改为真实第三通道「纸质简历扫描(OCR)」,选中态移到手机扫码上传;≤10MB→≤20MB(与后端 resume_upload 一致)
5. 11 详情页 h1 44px 折行孤字 → 页内 36px;actionbar 四按钮 nowrap 后回收宽度
6. 14 AI服务记录副文案「问答」暗示会话持久化(真实产品不持久化小青会话)→ 改「建议、面试、规划」
7. 15 「返回首页」114×45<48px → min-height:48;协议行 40px → min-height:52+cursor
8. 12(自查关联)办理双按钮折行 → 已修
9. 25/24/29/30 等 pagehead/卡内按钮折行(检测扫出)→ 已修

代表性拒绝(有据):
- 「214 个按钮无 onclick、跨屏断链」——本套为高保真**静态**原型,交互路由不在本轮范围(任务书口径:布局/文案/合规)
- 07 「所有文字均可编辑」——grep 证实该文案仅存在于 HTML 注释,用户不可见
- 20 收藏缺来源/同步/外部ID——页面 notice 已明示「来源机构、同步时间与外部ID在对应详情页查看」,收藏为轻量书签,合规且诚实
- 21 「权益消费未实现」——依据过时:C-2D 会员资产中心真实管理已完成(CLAUDE.md 阶段2)
- 08 「移除线上/线下双轨」——线下机构岗位为本轮定版产品方向(74/75 屏配套),不删
- 09 「二维码不可扫」——任务书零外部依赖,全套 QR 均为 CSS 示意
- 02/03/04/16/23/42 等「补齐游客/失败/离线状态变体」——单屏单态高保真稿的固有边界,不在本轮膨胀

### 段2(26–50):30 条 → 采纳 14 / 拒绝 16
采纳并修复:
1. 31 CTA「进入打印预览 · 遮挡 1 项」误导已脱敏 → 「遮挡选择已记录」(页内已有"仍打印原文件"红条)
2. 49 「现场签到 98」归入本终端服务统计,与 43 及合规口径(只记录浏览/外跳/打印)冲突 → 改「外部跳转」
3. 49 「招聘岗位 380 个·合计 1,120 人次」单位错 → 「计划招聘 1,120 人 · 主办方提供」;「准实时」→「数据定时更新」;参展企业注「主办方同步」
4. 43 「打开签到码」似按钮非按钮 → 「来源平台签到码」×2
5. 38 content 溢出 170px(实测证实)→ 已修
6. 39 「正在提问…/等待你开始回答/已输入半段答案」三态叠加 → 「本题已提问完毕」+「作答中 · 文字输入」
7. 41 「共 8 问」实际 5 项 → 「精选 5 问」
8. 26 「点击任意文本可直接修改」+预置 PDF 已生成 → 「轻触段落进入编辑;内容修改后需重新生成 PDF 再打印」
9. 27 解析等待无取消 → actionbar 增「取消解析」ghost 按钮
10. 30 「正在准备/已生成/生成可打印版」状态混乱 → 「当前材料」+「重新生成可打印版」
11. 33 index 承诺满意度缺失 → fb-card 增「本次体验:满意/一般/不满意」轻量评价行
12. 37 「已按有效期临时保存,登录后可长期管理」与未保存提示矛盾 → 「保存到我的文档 · 当前为临时保存;登录并保存后可长期管理」
13. 45 单 CTA 对应三岗位歧义 → qr-sub「前往来源平台查看该企业岗位并投递」
14. 48 「出发前逐项勾选」但为静态方框 → 「出发前逐项核对」

代表性拒绝:
- 44 搜索框非 input / 50 五 Tab 同内容 / 29 模板卡不可聚焦 / 34 aria 语义 / 42 多态同屏(index 自述「空态演示」为有意设计)——静态原型边界
- 64 「Word 可直接预览打印」——原文实为「Word 文档**暂不支持**页内预览」,指控与文件相反
- 32 退出支付无确认——页面已有「退出后订单超时自动关闭,不会扣款」+「勿重复扫码等待查单」双说明
- 28/40/47 等 index 描述过期类——任务书禁止改 index.html,已在本报告记录备查
- 25 页 meta viewport——1080×1920 固定舞台由 shared.js 缩放,非本轮范围

### 段3(51–75):21 条 → 采纳 11 / 拒绝 10
采纳并修复:
1. 52 「一寸照片拍摄/打印·本机即可拍摄打印」与 68「证件照即将上线」矛盾 → disabled 磁贴「证件照排版打印 · 即将上线;当前可用手机照片在打印扫描页打印」
2. 69 「证件照拍摄/打印」同上 → disabled+即将上线
3. 65 遮挡未生效仍「按以上设置打印」 → 「按以上设置打印原文件」
4. 62 「支持 PDF/Word/图片,≤10MB」与后端白名单不符(print=PDF/JPG/PNG,resume 另支持 Word,均 20MB)→ 「打印支持 PDF / JPG / PNG,简历另支持 Word;单个最大 20MB」
5. 63 手机号/验证码输入缺语义 → type=tel + inputmode=numeric + autocomplete=tel / one-time-code + maxlength
6. 55 「已获本人授权」无撤回口 → 补「可在『我的-账号设置』撤回授权」
7. 51 「6 项服务可用」硬编码语气 → 「本校已开通 6 项服务」
8. 66 首末位移动按钮仅 .dis 类无 disabled → 补 disabled 属性 ×2
9. 57 屏保小字 0.65–0.75 透明度对比不足 → 提到 0.8–0.85
10. 56 两段简历依据引文无分隔 → 加顿号
11. 60/73 弹层圆角 24/26px 超 V1 上限 18px → var(--r-md)

代表性拒绝:
- 73 「用真实照片顾问替换 CSS 字符」——任务书明令**禁照片**,拒绝
- 74/75 「标注为概念页/待数据接入」——全套 75 屏均为示例数据的设计提案,单独标注两屏反而制造双重标准;08 已含线下机构行与距离(直线)口径
- 53 「恢复关键词输入+游标加载」——原型对真实筛选契约做合理简化,`已显示 5/28`+分页为自洽示意
- 54/57/62/63 各类状态变体补齐——静态单态稿边界
- 63/64 placeholder 对比度(3.62:1)——涉及 shared 全局占位色,收益低风险面大,记录备查不改

## 阶段4:终验证据

- **grep 终检(正本目录全量)**:违禁词(一键投递|立即投递|企业收简历|候选人管理)0 命中;「平台投递」非白名单 0;「面试邀约」仅 3 处否定式声明(38/40/42「不参与企业筛选、面试邀约或录用决策」);外链(href/src=http)0;型号硬编码(pantum|cm2800|cm2820|奔图)0。
- **75 屏全量复测**:三批(25×3)FINAL CLEAN——出界 0 / 溢出 0 / 卡内裁切 0 / 按钮折行 0(容差聚类 Range 检测)。
- 抽屏截图核查:01(全部磁贴可见)、06(按钮单行、雷达完整)、08、10、32、40、46、53、64、74 均排版正常。
- worktree 快照 `.proto-review/` 已删除;worktree 仅剩会话开始前已存在的 `.claude/launch.json` 改动。
- shared.css 变更共 3 处(chip b 修复、back-btn nowrap、actionbar btn nowrap),均已随 75 屏全量复测回归。

## 残余待办(本轮范围外,移交决策)

1. index.html 描述与屏内实际的过期项(28 导出守门页、40 等级制报告、41 打印手册、47 批量→逐份、05 三通道名称)——任务书禁改 index,建议下轮统一校订。
2. 静态原型交互化(路由 <a href>、状态变体稿)是否立项,由用户决定。
3. shared 占位符颜色对比度(3.62:1)如需 WCAG 达标可整体调一次。
