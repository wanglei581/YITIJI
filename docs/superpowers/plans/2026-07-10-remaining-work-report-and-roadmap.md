# 项目现状开发报告 + 剩余工作开发计划（2026-07-10）

> 本文档只做「现状盘点 + 分轨道路线图」，不是可直接逐行执行的 bite-sized TDD 计划。
> 原因：剩余工作横跨多个互相独立的子系统（支付/打印扫描/AI简历/工程重构/真机验收），
> 按 `CLAUDE.md` §8.1「一窗口=一任务=一分支」「禁止大范围重写」的铁律，
> 每条轨道必须单独开分支、单独写范围、单独验证，不能合并成一次改动。
> 本文档产出后，需用户从 §3 选定 1 条轨道，再为该轨道单独产出可执行的 bite-sized 实施计划。

## 0. 结论先行

- 核心业务闭环（前台四板块 + 三端后台 + 硬件适配 Phase 8）已封板，当前主战场是**支付域收口**和**打印扫描"首期全功能商用版"**两条大轨道。
- 支付域（C-5 全系列 + W-A/W-B/W-C）代码侧已基本做完，**卡在"合并 main + 真实商户 live 冒烟 + 运营模式决策"**，不是缺代码。
- 打印扫描"首期全功能商用版"只完成了安全底座，**服务中心、扫码上传、AI体检、真实扫描、证件照、U盘、格式转换、Admin商业化后台全部是空白**，是当前唯一体量最大的纯代码缺口。
- 有相当一部分剩余项（Windows 真实扫描、证件复印、U 盘、断网恢复）**必须在 Windows 真机上验证**，本 macOS worktree 只能写 Terminal Agent 侧代码，无法完成真机验收。
- **文档滞后已修正（2026-07-10）**：核实 git 历史发现 `feature/payment-w-c` 早已随 **PR #174**（`3b1577b2`）合入 main，`#175` 微信退款回调通知也已随 PR 合入 main（`b8263be2`）；`current-progress.md`/`next-tasks.md` 之前的"未合 main"表述是过时的，已更新为已合并状态。轨道 A 的"合并 W-C"动作实际不需要执行，只需修正文档。

---

## 1. 开发报告：当前完整状态

### 1.1 已完成并稳定的基线（无需再碰）

- Phase 1–7：设计系统、Kiosk 前台、Admin/Partner 后台、岗位/招聘会真实 API、AI Provider 骨架
- Phase 8：Windows Terminal Agent 全部封板（DPAPI/SQLite/WMI/单实例/断网重试/Windows 服务），2026-07-06 现场物理出纸已通过（计数器 27→30 + PrintService Event 307/842 + 现场人工确认）
- 三端登录 + 内部手机号认证、政策服务页重构、UI 换装（inkpaper/fusion-youth，42+28 页）、智慧校园、百宝箱微应用平台底座、文件资产保存期限体系、AI 简历诊断（真实百度 OCR + LLM）
- 我的页商用闭环第一批（P0a/P0b/P1）：AI服务记录、打印订单详单/取件码、权益核销落库

### 1.2 支付域 C-5（当前最活跃、最接近收口的轨道）

| 批次 | 内容 | 状态 |
|---|---|---|
| C5-1 | Order/PriceConfig 数据底座 + fail-closed 计价 | ✅ 已合 main |
| C5-2 | 沙箱线上支付闭环（PaymentAttempt+回调验签+幂等） | ✅ 已合 main |
| C5-3 | Kiosk 收银 UI + paid-before-claim 出纸门控 | ✅ 已合 main |
| C5-4 | 退款域 + 核销域（RedemptionRecord 扩展） | ✅ 已合 main |
| C5-6 | 微信/支付宝真实渠道适配（Native APIv3/当面付） | ✅ 代码+verify 通过，**未合 main** |
| W-A | 价格单一真相源（Kiosk 估价与服务端同源） | ✅ 已合 main (#170) |
| W-B | 真实渠道退款（原路退回，结果三分法） | ✅ 已合 main (#171) |
| #175 | 微信退款结果回调通知接口 | ✅ **已合 main（今日），未写入进度文档** |
| W-C part1/2a/2b | Admin 计费配置 + 本地对账 + 退款自动收敛 + 前端页 | ✅ 代码+verify 通过，**分支未合 main** |
| W-C part2c | 渠道账单拉取 diff、Admin 订单页退款接线 | ⬜ 未开工（标记"择期/部署期"） |
| C5-7 | 商用验收：全链路+合规复核+预生产冒烟 | ⬜ 未开工 |

**支付域下一步不缺代码，缺三件事**：① 把 `feature/payment-w-c` 合入 main；② 真实商户凭证 + 域名 HTTPS + 1 分钱 live 冒烟（部署期动作，非编码）；③ 拍板首台试运营用"免费模式"还是"有人值守线下收款"。

### 1.3 打印扫描首期全功能商用版（体量最大的纯代码缺口）

2026-06-30 用户确认目标是"全功能商用版"，但目前只完成了**安全底座**（任务绑定终端、Agent 降级 fail-closed、claim 二道闸门）。以下全部是**空白，需要从零写代码**：

- 服务中心入口 `/print-scan` + FeatureGate/DeviceCapability 能力开关体系
- 手机扫码上传 / 云上传 / 安全取件码
- AI 文件体检（清晰度/空白页/敏感信息检测）+ 材料包组合打印
- **真实扫描**（TWAIN/WIA 或扫描目录监听）—— 需 Windows 真机
- 证件复印（身份证正反面合成）+ 证件照（抠图/换底色/规格检测）—— 涉及高敏个人信息，需先过合规
- U 盘导入（Agent 只读枚举）—— 需 Windows 真机
- 格式转换（Word/图片→PDF）与签名盖章（非 CA 电子签，需免责声明）
- Admin 商业化运营后台（统一任务中心、权益券、异常处理、账单看板）

### 1.4 AI 简历优化（6 波）

Wave 1–4（结构化优化目标、排版参数化、模板库、语音生成）已完成并预生产验证。**Wave 5（收费闭环）未立项**，**Wave 6（岗位URL定向+格式转换+真机出纸）未做**。

### 1.5 工程规范化拆分

- N1/N3（`jobs.service.ts` 2316 行、`terminals.service.ts` 1182 行拆分）**被阻塞**：需等 toolbox/terminal-device 分支合入 main 后才能排期
- N5/N6（`materials.service.ts` 850 行、`admin-fairs.service.ts` 811 行拆分）未开工

### 1.6 上线前 P0 阻塞项（非新功能，是验收缺口）

生产域名 HTTPS、PostgreSQL 生产实例迁移、Redis 生产连接、COS 生产私有桶、zyidai.cn 数据库高负载加固**生产执行**（代码已就绪，未执行）、腾讯短信 live、百度 OCR/AI/TRTC 生产密钥轮换、Windows 真机扫描链路（完全空白）、断网/重启异常场景补测、法务合规文案审定。

---

## 2. 剩余工作分轨道（每条轨道 = 未来单独一个分支）

按 `CLAUDE.md` §8.1 要求，每条轨道落地前必须单独确认：对应闭环/上线阻塞点、允许改动文件范围、是否新增入口、是否触碰岗位/简历/打印/生产/密钥/硬件、需要的验证方式、是否需要同步 `docs/progress/*`。以下只给出轨道级别的范围和优先级，具体 bite-sized 步骤留到选定轨道后单独产出。

### 轨道 A｜文档同步 + W-C 合并（最小、最快，建议先做）
- **范围**：补记 #175 到 `current-progress.md`/`next-tasks.md`；review 并合并 `feature/payment-w-c` 到 main。
- **不碰**：业务代码、数据库 schema。
- **风险**：低。**是否需要真机**：否。
- **产出**：文档与代码状态一致，W-C 三个 verify 门禁（admin-billing/reconciliation/refund-convergence）进 main CI。

### 轨道 B｜打印扫描服务中心 + 手机扫码上传/云上传/安全取件（纯软件，可在本会话完整交付）
- **范围**：`/print-scan` 服务中心页面（首页入口 + FeatureGate 开关）、手机扫码上传流程（会员/一次性会话绑定当前终端、签名 URL 过期清理）、安全取件码复用现有 pickupCode 机制。
- **不碰**：不涉及物理扫描仪、证件照、U盘（那些需要 Windows 真机，见轨道 D）。
- **是否新增入口**：新增 `/print-scan` 服务中心一个入口，二级能力沿用现有打印任务模型。
- **风险**：中。**是否需要真机**：否，可在浏览器预览中完整验证。
- **推荐理由**：这是"首期全功能"里唯一**不需要 Windows 硬件就能做成完整可演示闭环**的部分，适合作为本次会话的交付目标。

### 轨道 C｜AI 文件体检 + 材料包
- **范围**：上传文件后端 AI 检测（清晰度/方向/空白页/敏感信息标记）、材料包多文件组合选择与顺序打印、子任务失败单独重试。
- **依赖**：轨道 B 的上传流程作为前置。
- **风险**：中高（需要真实图像处理能力，涉及 LLM/OCR 调用成本）。

### 轨道 D｜真实扫描 + 证件复印/证件照 + U 盘导入（需 Windows 真机）
- **范围**：Terminal Agent 侧新增 TWAIN/WIA 调用或扫描目录监听；身份证正反面合成（高敏，需先过合规评审）；U 盘只读枚举。
- **强约束**：身份证/证件照采集必须走"采集→使用→删除→审计"真机验收，本 macOS 会话**无法验证**，只能写代码，验收需转 Windows 真机窗口。
- **建议**：先补合规评审文档，再排代码。

### 轨道 E｜格式转换 + 签名盖章
- **范围**：Word/图片→PDF 真实转换服务、隐私遮挡替换文件生成、签名盖章图形排版（非 CA 电子签免责声明）。
- **风险**：中。**是否需要真机**：转换本身不需要，最终打印验证需要。

### 轨道 F｜Admin 商业化运营后台
- **范围**：统一任务中心（打印/扫描/复印/证件照/材料包）、取消/重试/释放卡住任务、权益券/免费额度/补贴核销运营视图、文件生命周期看板。
- **依赖**：轨道 B/C/D/E 的数据模型先落地，此轨道才有真实数据可展示。

### 轨道 G｜AI 简历 Wave 5（收费闭环）
- **范围**：需先独立立项——计费属性定义、Package/Plan/SKU + PricingRule、复用已有支付域基础设施、权益核销、Admin 价格/额度/退款后台。
- **依赖**：强依赖支付域（轨道 A 完成后的 main）。

### 轨道 H｜AI 简历 Wave 6（岗位 URL 定向 + 格式转换 + 真机出纸）
- **范围**：岗位 URL 收窄为合作来源白名单/用户手动粘贴 JD；docx/txt/md → PDF 后可打印；Windows 真机出纸验收。

### 轨道 I｜工程规范化拆分 N1/N3/N5/N6
- **范围**：`jobs.service.ts`/`terminals.service.ts`/`materials.service.ts`/`admin-fairs.service.ts` 按职责拆分，零行为变更。
- **前置条件**：N1/N3 需等 toolbox/terminal-device 分支先合 main。

### 轨道 J｜生产真机验收收尾
- **范围**：Windows 真机扫描、断网/重启恢复、法务合规文案审定、生产基础设施（域名/HTTPS/PostgreSQL/Redis/COS/短信/OCR密钥）逐项验收。
- **性质**：主要是验收执行，不是新代码；**必须在 Windows/生产环境执行，本会话无法代为完成**。

---

## 3. 优先级建议与本次会话决策点

建议顺序：**A（文档同步，5 分钟）→ B（打印扫描服务中心+扫码上传，本会话可完整交付）→ 其余轨道按用户业务优先级排期**。

C-5 支付域"收口"更多是合并+部署+运营决策，不是缺代码；而 D/F/J 强依赖 Windows 真机，本会话做不了验收。轨道 B 是当前唯一"体量适中、纯软件、能在本会话做成一个从前端到后端完整可验证闭环"的选项，最贴合你说的"最后做成一个完整的功能出来"。

**需要你决定**：
1. 是否同意先做轨道 A（文档同步+合并 W-C），再做轨道 B 作为本次"完整功能"交付？
2. 若不选轨道 B，你更想优先做哪一条（C/D/E/F/G/H/I/J 或支付域相关的部署期动作）？
