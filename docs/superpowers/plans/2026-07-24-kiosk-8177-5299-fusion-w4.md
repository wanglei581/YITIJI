# Kiosk 8177 / 5299 Fusion W4 Jobs, Fairs, Campus, and Policy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将岗位、企业、线下机构、招聘会、校园招聘、智慧校园与政策服务的 23 条生产路由迁移到冻结的 5299 主导、8177 状态补全融合呈现，同时完整保留真实数据、来源跳转、浏览记录、打印与合规边界。

**Architecture:** 从 W1 基线 `b19d71f5` 出发，先用独立 Node verifier 冻结精确路由、文件边界与行为标记，再把两个旧 CSS 入口改为兼容聚合器，按 jobs/companies/fairs/campus/smart-campus/renshi 串行迁移。页面只组合 W1 的 `KioskPageFrame`、`KioskPageHeader`、`KioskStatePanel`、`KioskActionBar`、`KioskModal`，不改服务、DTO、路由或共享组件；最后以真实 HTTP envelope fixture 和 fail-closed Playwright 跑代表性闭环。

**Tech Stack:** React 18, React Router 7, TypeScript 5.7, Vite 6, `@ai-job-print/ui` frozen Kiosk primitives, Node 22 ESM verifier, Playwright 1.54.2 Chromium.

---

## 0. 基线、精确 ownership 与硬边界

执行前必须确认：

```bash
git rev-parse --short=8 HEAD
git status --short
```

Expected: HEAD 为 `b19d71f5`；除已知的其他 wave 计划草稿外，W4 业务文件无未提交改动。若 HEAD 不同，先确认它是 W1 后续纯文档提交；不得把其他 wave 的业务提交混入 W4。

### 精确 23 条路由

```ts
export const W4_ROUTES = [
  '/jobs',
  '/jobs/:id',
  '/jobs/:id/offline',
  '/offline-agencies',
  '/companies',
  '/companies/:id',
  '/job-fairs',
  '/job-fairs/checkin',
  '/job-fairs/:id',
  '/job-fairs/:id/companies',
  '/job-fairs/:id/companies/:companyId',
  '/job-fairs/:id/map',
  '/job-fairs/:id/materials',
  '/job-fairs/:id/visit-plan',
  '/job-fairs/:id/stats',
  '/campus',
  '/campus/welcome',
  '/campus/freshman-insights',
  '/smart-campus',
  '/smart-campus/welcome',
  '/smart-campus/freshman-insights',
  '/smart-campus/service/:key',
  '/renshi',
] as const
```

`/notifications` 归 W5，不能在 W4 verifier、页面迁移或浏览器场景中认领。`/offline-agencies/:id` 不在生产路由全集中；W4 不新增它，也不保留任何指向它的按钮。

### 允许改动的生产文件

- `apps/kiosk/src/pages/jobs/**`，但 `utils/jobDisplay.ts` 只读。
- `apps/kiosk/src/pages/companies/**`。
- `apps/kiosk/src/pages/offline-agencies/**`。
- `apps/kiosk/src/pages/job-fairs/**`。
- `apps/kiosk/src/pages/campus/**`。
- `apps/kiosk/src/pages/smart-campus/**`。
- `apps/kiosk/src/pages/renshi/**`，但 `builtinData.ts`、`shared.ts` 只读。
- `apps/kiosk/src/pages/jobs-fairs-prototype.css`。
- `apps/kiosk/src/pages/prototype/kiosk-prototype.css`。
- `apps/kiosk/src/pages/placeholders/CampusWelcomePage.tsx`。
- `apps/kiosk/src/pages/placeholders/FreshmanInsightsPage.tsx`。
- 本计划明确创建的 W4 verifier、CSS、Playwright config/spec/fixture。

### 硬冻结文件与目录

```text
apps/kiosk/src/routes/**
apps/kiosk/src/services/**
packages/shared/**
services/**
apps/kiosk/package.json
.github/workflows/ci.yml
apps/kiosk/src/index.css
apps/kiosk/src/components/ComingSoonNotice.tsx
apps/kiosk/src/pages/home/components/ToolboxLaunchModals.tsx
apps/kiosk/src/pages/home/components/kioskAppLaunch.ts
apps/kiosk/src/pages/jobs/utils/jobDisplay.ts
apps/kiosk/src/pages/renshi/builtinData.ts
apps/kiosk/src/pages/renshi/shared.ts
```

`ComingSoonNotice.tsx` 当前零 consumer，W4 不修改也不引入。`ToolboxLaunchModals.tsx` 同时服务 toolbox 与 smart-campus，W4 只保留 `SmartCampusHomePage` 的既有 import/use，不修改共享文件。

与并行 W2/W3/W5 的共享边界如下：W1 的 `packages/ui/**` primitives 是冻结依赖，不由 W4 修改；W2 拥有打印/扫描页面，W4 只按既有 state contract 导航 `/print/confirm`；W3 拥有简历、岗位匹配与 AI 服务，W4 保留 jobs 内已有 AI consumer 但不改任何 AI service/DTO；W5 拥有 home/toolbox/profile/system，W4 只在 smart-campus 继续调用冻结的 toolbox launch helpers。`apps/kiosk/src/index.css`、`apps/kiosk/package.json` 和 CI 统一留给 W6 集成阶段，W4 不登记 script、不改变全局 import 顺序。

### 永久行为约束

- 岗位保留“线上来源平台 + 线下机构门店”双轨；平台不接收简历。
- 岗位/企业详情保留 `recordBrowse` 与 `recordExternalJump`；终端显示二维码，非终端打开 HTTPS 来源页时保持 `noopener,noreferrer`。
- 允许 CTA 只有 `扫码投递`、`去来源平台投递`、`扫码预约`、`去来源平台预约`、`扫码前往来源平台签到` 等来源语境文案。
- 招聘会签到只表示前往来源平台，本系统不记录签到结果。
- `isMockData` 在详情与统计页必须显示“暂无真实统计”状态，不渲染模拟数字。
- 活动资料与参会准备单只把后端 `printFileUrl` 交给打印链路。
- `/campus/*` 与 `/smart-campus/*` 语义独立；前者是校园招聘专区及直达容错页，后者是智慧校园能力。
- `/smart-campus/freshman-insights` 始终锁定；学校书面授权、数据处理协议、聚合脱敏统计三项全部满足前，不展示统计。
- `/renshi` 只做信息与官方入口，不代办、不保证结果；内置条目无伪造更新时间、不收藏、不上报，库内条目保留收藏和上报。
- 每个实现 commit 最多 12 个文件；每个任务先 RED/契约检查，再写最小实现。

## Task 1: 用 RED verifier 冻结 W4 路由、scope 与行为契约

**Files:**
- Create: `apps/kiosk/scripts/verify-fusion-w4.mjs`

- [ ] **Step 1: 创建可直接执行的 verifier**

脚本必须从 `import.meta.url` 解析 kiosk 根目录，使用 `node:assert/strict`、`node:fs`、`node:path`、`node:child_process`，并实现以下精确结构：

```js
const W1_BASELINE = 'b19d71f554c44066a0e61bc53d2497913a279cb8'
const W4_ROUTES = [
  '/jobs', '/jobs/:id', '/jobs/:id/offline', '/offline-agencies',
  '/companies', '/companies/:id', '/job-fairs', '/job-fairs/checkin',
  '/job-fairs/:id', '/job-fairs/:id/companies',
  '/job-fairs/:id/companies/:companyId', '/job-fairs/:id/map',
  '/job-fairs/:id/materials', '/job-fairs/:id/visit-plan',
  '/job-fairs/:id/stats', '/campus', '/campus/welcome',
  '/campus/freshman-insights', '/smart-campus', '/smart-campus/welcome',
  '/smart-campus/freshman-insights', '/smart-campus/service/:key', '/renshi',
]
const OWNED_PREFIX = /^(jobs(?:\/|$)|offline-agencies(?:\/|$)|companies(?:\/|$)|job-fairs(?:\/|$)|campus(?:\/|$)|smart-campus(?:\/|$)|renshi$)/
const FORBIDDEN_PATHS = [
  /^services\//, /^packages\/shared\//, /^apps\/kiosk\/src\/services\//,
  /^apps\/kiosk\/src\/routes\//, /^apps\/kiosk\/package\.json$/,
  /^\.github\/workflows\/ci\.yml$/, /^apps\/kiosk\/src\/index\.css$/,
  /^apps\/kiosk\/src\/components\/ComingSoonNotice\.tsx$/,
  /^apps\/kiosk\/src\/pages\/home\/components\/(ToolboxLaunchModals|kioskAppLaunch)\.tsx?$/,
  /^apps\/kiosk\/src\/pages\/jobs\/utils\/jobDisplay\.ts$/,
  /^apps\/kiosk\/src\/pages\/renshi\/(builtinData|shared)\.ts$/,
]
const PLANNED_TEST_FILES = new Set([
  'docs/superpowers/plans/2026-07-24-kiosk-8177-5299-fusion-w4.md',
  'apps/kiosk/scripts/verify-fusion-w4.mjs',
  'apps/kiosk/playwright.w4.config.ts',
  'apps/kiosk/tests/fixtures/fusion-w4-api.ts',
  'apps/kiosk/tests/visual/fusion-w4.spec.ts',
])
```

脚本使用 TypeScript compiler API 解析 `src/routes/index.tsx`，定位 `createBrowserRouter([...])` 后只读取路由对象的直接 `path` 字符串属性；不得使用 whole-file regex 提取路由。规范化前导 `/` 后只保留 `OWNED_PREFIX`；源码顺序和实施域顺序不同，因此使用 `assert.deepEqual([...owned].sort(), [...W4_ROUTES].sort())` 比较集合，并另外断言无重复、结果不含 `/notifications` 与 `/offline-agencies/:id`、总数严格为 23。

scope 检查必须合并以下输出，并验证每个 W4 改动路径满足“本节允许改动的生产前缀/精确文件”或 `PLANNED_TEST_FILES`；命中 `FORBIDDEN_PATHS` 必须立即失败，其他 wave 的 `docs/superpowers/plans/*-w2.md`、`*-w3.md`、`*-w5.md` 无论已提交还是未跟踪都只忽略、不认领：

```bash
git diff --name-only b19d71f554c44066a0e61bc53d2497913a279cb8
git ls-files --others --exclude-standard
```

任何既不在 W4 allowlist、也不是上述其他 wave 计划文档的路径都要报 scope violation；不能只检查 forbidden list。随后读取 W4 源码并断言：

```js
assert.match(jobsPage, /KioskPageFrame/)
assert.match(jobDetail, /recordBrowse[\s\S]*'job'/)
assert.match(jobDetail, /recordExternalJump[\s\S]*'external_apply'/)
assert.match(jobDetail, /扫码投递/)
assert.match(jobDetail, /去来源平台投递/)
assert.doesNotMatch(offlineAgencies, /offline-agencies\/\$\{agency\.id\}/)
assert.match(companyDetail, /recordBrowse[\s\S]*'company_profile'/)
assert.match(companyDetail, /recordExternalJump/)
assert.match(jobFairDetail, /external_appointment/)
assert.match(jobFairDetail, /external_checkin_open/)
assert.match(jobFairDetail, /!stats\.isMockData/)
assert.match(fairMaterials, /printable\.printFileUrl/)
assert.doesNotMatch(fairMaterials, /fileUrl:\s*material\.fileUrl/)
assert.match(fairStats, /stats\.isMockData/)
assert.match(campusPage, /getJobFairs\(terminalId \? \{ terminalId \} : undefined\)/)
assert.doesNotMatch(campusWelcome, /待开发/)
assert.doesNotMatch(campusInsights, /待开发/)
assert.match(smartCampusHome, /ToolboxLaunchModals/)
assert.match(smartCampusHome, /placement="smart_campus"/)
assert.match(smartCampusInsights, /学校书面授权/)
assert.match(smartCampusInsights, /数据处理协议/)
assert.match(smartCampusInsights, /聚合脱敏统计/)
assert.doesNotMatch(smartCampusInsights, /示例数据|MOCK_FRESHMAN|topMajors|ageDistribution/)
assert.match(renshiPage, /if \(isBuiltin\(item\.id\)\) return/)
assert.match(renshiPage, /if \(!isBuiltin\(item\.id\)\) recordExternalJump/)
assert.match(jobsCss, /@import ['"]\.\/styles\/jobs-fairs-foundation\.css['"]/)
assert.match(jobsCss, /@import ['"]\.\/styles\/jobs-companies-fusion\.css['"]/)
assert.match(jobsCss, /@import ['"]\.\/styles\/job-fairs-fusion\.css['"]/)
assert.match(jobsCss, /@import ['"]\.\/styles\/campus-policy-fusion\.css['"]/)
assert.doesNotMatch(w4PageSource, /import ['"][^'"]*(?:jobs-fairs-prototype\.css|prototype\/kiosk-prototype\.css)['"]/)
assert.doesNotMatch(w4PageSource, /\b(?:Proto[A-Z]\w*|CardHead|SourceMetaChips)\b/)
```

`w4PageSource` 只拼接 W4-owned 页面与两个 campus placeholder，不含历史只读的 `jobs-fairs-prototype.tsx`。递归读取 `apps/kiosk/src/**/*.tsx`，断言除组件定义文件本身外没有 `ComingSoonNotice` import；对 jobs/companies/job-fairs/campus/renshi 可见源码先移除允许来源短语，再禁止 `一键投递|立即投递|平台投递|投递简历|候选人管理|面试邀约|签到成功|确认签到|保证到账|免申即享`。

- [ ] **Step 2: 运行 RED**

```bash
node apps/kiosk/scripts/verify-fusion-w4.mjs
```

Expected: exit 1；至少报告 `JobsPage` 尚未使用 `KioskPageFrame`、两个 campus placeholder 仍含 `待开发`、CSS 聚合 import 尚不存在，以及线下机构列表仍指向不存在的 `/offline-agencies/${agency.id}`。

- [ ] **Step 3: 提交 RED 契约**

```bash
git add apps/kiosk/scripts/verify-fusion-w4.mjs
git commit -m "test(kiosk): define fusion w4 contract"
```

Expected: 1 file committed；verifier 仍为 RED。

## Task 2: 把旧 CSS 拆成 foundation + 域 CSS 兼容聚合入口

**Files:**
- Modify: `apps/kiosk/src/pages/jobs-fairs-prototype.css`
- Modify: `apps/kiosk/src/pages/prototype/kiosk-prototype.css`
- Create: `apps/kiosk/src/pages/styles/jobs-fairs-foundation.css`
- Create: `apps/kiosk/src/pages/styles/jobs-companies-fusion.css`
- Create: `apps/kiosk/src/pages/styles/job-fairs-fusion.css`
- Create: `apps/kiosk/src/pages/styles/campus-policy-fusion.css`

- [ ] **Step 1: 盘点所有 selector consumer，并保存临时证据而非仓库文件**

```bash
rg -o 'className="[^"]+"|className={`[^`]+`}|className=\{`[^`]+`\}' \
  apps/kiosk/src/pages/{jobs,companies,offline-agencies,job-fairs,campus,smart-campus,renshi} \
  apps/kiosk/src/pages/placeholders/{CampusWelcomePage,FreshmanInsightsPage}.tsx \
  | sort -u > /tmp/w4-class-consumers.txt
rg -n '^\s*[^@/][^{]+\{' \
  apps/kiosk/src/pages/jobs-fairs-prototype.css \
  apps/kiosk/src/pages/prototype/kiosk-prototype.css \
  > /tmp/w4-selectors-before.txt
```

Expected: 两个文件分别约 1640/509 行；consumer 覆盖 `jf-`、`oa-`、`kproto-` 以及页面专属 class。`/tmp` 文件不加入 Git。

- [ ] **Step 2: 按固定责任移动规则，不改声明**

分配规则如下，每条旧规则只进入一个目标文件：

```text
jobs-fairs-foundation.css:
  ProtoPage/ProtoBadge/ProtoNotice 基础；通用 .jf-btn/.jf-chip/.jf-filter-bar；
  共享颜色、排版、focus-visible、reduced-motion、1080×1920 公共响应式。

jobs-companies-fusion.css:
  jobs 的列表/详情/AI 面板；.oa-*；companies 列表/详情；仅这些页面使用的响应式。

job-fairs-fusion.css:
  招聘会列表、checkin、详情、企业/展位、地图、资料、准备单、统计；
  FairCalendar/RegionPicker/FairDataScreen/JobFairDetailTabs 使用的规则。

campus-policy-fusion.css:
  .kproto-* 壳层和 campus/smart-campus/renshi 页面；校园锁定态与政策弹层规则。
```

先逐字移动既有规则，再在各域文件末尾添加 fusion 页面 class；此步骤不重命名旧 selector，不引入 `!important`，不改变 z-index、sticky、scroll 或 touch target 行为。

- [ ] **Step 3: 将旧文件改成稳定聚合器**

`jobs-fairs-prototype.css` 只保留以下四行，顺序固定以维持 cascade：

```css
@import './styles/jobs-fairs-foundation.css';
@import './styles/jobs-companies-fusion.css';
@import './styles/job-fairs-fusion.css';
@import './styles/campus-policy-fusion.css';
```

`kiosk-prototype.css` 保持可被旧 consumer 直接 import，内容为：

```css
@import '../styles/jobs-fairs-foundation.css';
@import '../styles/campus-policy-fusion.css';
```

不得删除或修改 `apps/kiosk/src/index.css` 中现有的 `@import "./pages/jobs-fairs-prototype.css";`。W6 证明零 consumer 前，两个兼容入口都保留。

- [ ] **Step 4: 验证无规则丢失并提交**

```bash
rg -n '^\s*[^@/][^{]+\{' apps/kiosk/src/pages/styles/*.css > /tmp/w4-selectors-after.txt
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
```

Expected: verifier 仍因页面未迁移而 exit 1，但不再报告 CSS import；typecheck PASS；lint 0 errors。人工将 `/tmp/w4-selectors-before.txt` 的每个 selector 与 after 对照，旧规则无遗漏、无跨域重复。

```bash
git add apps/kiosk/src/pages/jobs-fairs-prototype.css \
  apps/kiosk/src/pages/prototype/kiosk-prototype.css \
  apps/kiosk/src/pages/styles
git commit -m "refactor(kiosk): split jobs fairs presentation styles"
```

Expected: 6 files committed。

## Task 3: 迁移岗位双轨、岗位详情与线下机构页面

**Files:**
- Modify: `apps/kiosk/src/pages/jobs/JobsPage.tsx`
- Modify: `apps/kiosk/src/pages/jobs/JobDetailPage.tsx`
- Modify: `apps/kiosk/src/pages/jobs/components/JobDetailSections.tsx`
- Modify: `apps/kiosk/src/pages/jobs/components/JobResultsSection.tsx`
- Modify: `apps/kiosk/src/pages/jobs/components/JobAiConsentModal.tsx`
- Modify: `apps/kiosk/src/pages/jobs/components/ResumeSelectModal.tsx`
- Modify: `apps/kiosk/src/pages/offline-agencies/OfflineAgenciesPage.tsx`
- Modify: `apps/kiosk/src/pages/offline-agencies/OfflineJobDetailPage.tsx`
- Modify: `apps/kiosk/src/pages/styles/jobs-companies-fusion.css`

- [ ] **Step 1: 先运行岗位门禁确认现状为 GREEN**

```bash
pnpm --filter @ai-job-print/kiosk verify:job-info-ui
pnpm --filter @ai-job-print/kiosk verify:job-ai-ui
pnpm --filter @ai-job-print/kiosk verify:job-ai-history-privacy-ui
```

Expected: 三条均 PASS；若任一失败，先停止，不能用视觉迁移掩盖基线行为问题。

- [ ] **Step 2: 用 W1 primitives 替换岗位页面壳与状态**

`JobsPage` 与 `JobDetailPage` 的最外层统一为：

```tsx
<KioskPageFrame
  className="w4-jobs-page"
  header={<KioskPageHeader title={title} description={description} onBack={onBack} backLabel={backLabel} aside={badge} />}
  footer={<KioskActionBar leading={complianceNote}>{actions}</KioskActionBar>}
>
  <main className="w4-jobs-content">{content}</main>
</KioskPageFrame>
```

删除 W4 页面对 `../jobs-fairs-prototype`、`../jobs-fairs-prototype.css` 的运行时依赖；`jobs-fairs-prototype.tsx` 是 W4 范围外的历史只读文件，留给 W6 在全仓零 consumer 证明后处理。

`JobsPage` 当前从旧 helper 取得的 `formatDate` 改为页面内纯展示 formatter：无值返回“暂无同步时间”，合法 ISO 使用 `Intl.DateTimeFormat('zh-CN', { year: 'numeric', month: '2-digit', day: '2-digit' })`；不改变排序或 API query。

loading/error/empty 分支使用 `KioskStatePanel`，分别给出明确 title、description、重试按钮。保留 `getJobs` 的 100 条 facet 请求、服务端筛选、收藏、AI 授权/简历选择/推荐/解读/匹配、配额错误和所有原函数签名。

`JobAiConsentModal`、`ResumeSelectModal` 仅把现有弹层容器换成 `KioskModal`；保留隐私说明、只选解析完成简历、取消/确认 callback。不得改 `JobAiResultPanel` 的参考免责声明。

- [ ] **Step 3: 保留来源浏览/跳转契约**

`JobDetailPage` 必须继续保留：

```tsx
recordBrowse(getToken(), 'job', job.id)
recordExternalJump(getToken(), 'job', currentJob.id, 'external_apply')
window.open(currentJob.sourceUrl, '_blank', 'noopener,noreferrer')
```

终端主 CTA 为 `扫码投递` 并显示 `SourceUrlQr`；非终端可见 `去来源平台投递`。没有合法 `sourceUrl` 时两个 CTA disabled，并展示“来源入口暂不可用”，不能构造 URL。

- [ ] **Step 4: 诚实处理线下机构无详情路由事实**

`OfflineAgenciesPage` 保留真实 `getOfflineAgencies`、筛选、分页、统计和门店信息，但把 `AgencyRow` 从导航 button 改为无点击的 `<article>`；移除：

```tsx
navigate(`/offline-agencies/${agency.id}`)
```

卡片只显示机构资质、地址、营业时间、服务、岗位数和“岗位咨询请到店办理”。页面保留返回 `/jobs`；不新增 `/offline-agencies/:id`，不把 agency id 冒充 job id。

`OfflineJobDetailPage` 仍由合法 `/jobs/:id/offline` 加载 `getOfflineJobDetail(id)`；将 `alert('打印功能即将上线')` 删除，底部只保留返回岗位信息和诚实的到店咨询说明。若提供打印入口，唯一允许形式是导航 `/print/upload` 且标签为“上传自备材料打印”，不得声称已经生成岗位文件。

- [ ] **Step 5: 跑 GREEN 并提交**

```bash
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk verify:job-info-ui
pnpm --filter @ai-job-print/kiosk verify:job-ai-ui
pnpm --filter @ai-job-print/kiosk verify:job-ai-history-privacy-ui
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
```

Expected: 岗位三门禁、typecheck、lint PASS；W4 verifier 只因后续 domain 未完成而 exit 1，不再报告岗位、线下机构或不存在路由。

```bash
git add apps/kiosk/src/pages/jobs/JobsPage.tsx \
  apps/kiosk/src/pages/jobs/JobDetailPage.tsx \
  apps/kiosk/src/pages/jobs/components/JobDetailSections.tsx \
  apps/kiosk/src/pages/jobs/components/JobResultsSection.tsx \
  apps/kiosk/src/pages/jobs/components/JobAiConsentModal.tsx \
  apps/kiosk/src/pages/jobs/components/ResumeSelectModal.tsx \
  apps/kiosk/src/pages/offline-agencies/OfflineAgenciesPage.tsx \
  apps/kiosk/src/pages/offline-agencies/OfflineJobDetailPage.tsx \
  apps/kiosk/src/pages/styles/jobs-companies-fusion.css
git commit -m "feat(kiosk): migrate fusion job information pages"
```

Expected: 9 files committed；`utils/jobDisplay.ts` 不在 commit 中。

## Task 4: 迁移企业列表与企业详情

**Files:**
- Modify: `apps/kiosk/src/pages/companies/CompaniesPage.tsx`
- Modify: `apps/kiosk/src/pages/companies/CompanyDetailPage.tsx`
- Modify: `apps/kiosk/src/pages/styles/jobs-companies-fusion.css`

- [ ] **Step 1: 写下并检查不可丢失的静态契约**

```bash
rg -n "getCompanies|getCompanyStats|getCompanyById|getCompanyJobs|recordBrowse|recordExternalJump|external_apply" \
  apps/kiosk/src/pages/companies
```

Expected: 列表同时调用列表与真实聚合；详情加载企业与岗位、记录企业浏览、企业官方入口跳转和岗位来源跳转。

- [ ] **Step 2: 迁移列表/详情呈现**

两页使用 `KioskPageFrame` + `KioskPageHeader`，loading/error/empty 使用 `KioskStatePanel`。保留游标分页、行政区/企业类型/行业/招聘类型/来源筛选，以及后端开关过滤后的指标；不得写死企业数、岗位数或城市统计。

同时删除两页对 `../prototype/kiosk-prototype.css` 的直接 import；样式由 `src/index.css → jobs-fairs-prototype.css → jobs-companies-fusion.css` 的兼容聚合链提供。

企业详情文案固定为“来源企业与岗位导览”，不能出现企业端候选人、收简历或录用流程。企业官网只接受现有 DTO 的 HTTPS URL；岗位 CTA 仍通过 job source URL 使用 `external_apply`，并使用 `扫码投递`/`去来源平台投递`。

- [ ] **Step 3: 验证并提交**

```bash
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk verify:job-info-ui
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
git add apps/kiosk/src/pages/companies/CompaniesPage.tsx \
  apps/kiosk/src/pages/companies/CompanyDetailPage.tsx \
  apps/kiosk/src/pages/styles/jobs-companies-fusion.css
git commit -m "feat(kiosk): migrate fusion company guide pages"
```

Expected: 3 files committed；门禁 PASS；W4 verifier 只剩 fairs/campus/policy 未完成项。

## Task 5: 迁移招聘会列表、来源签到、详情与参展企业链路

**Files:**
- Modify: `apps/kiosk/src/pages/job-fairs/JobFairsPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/JobFairCheckinPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/JobFairDetailPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/FairCompaniesPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/FairCompanyDetailPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/components/FairCompanyDetailSections.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/components/JobFairDetailTabs.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/components/FairCalendarPopover.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/components/RegionPicker.tsx`
- Modify: `apps/kiosk/src/pages/styles/job-fairs-fusion.css`

- [ ] **Step 1: 先跑招聘会 RED 防回退基线**

```bash
pnpm --filter @ai-job-print/kiosk verify:jobfair-ui
pnpm --filter @ai-job-print/kiosk verify:jobfair-commercial-closure
pnpm --filter @ai-job-print/kiosk verify:jobfair-checkin
node apps/kiosk/scripts/verify-jobfairs-terminal-priority.mjs
```

Expected: 四条全部 PASS。

- [ ] **Step 2: 迁移列表与来源签到**

`JobFairsPage` 使用 W1 page/header/state/action primitives，继续在请求前执行：

```ts
const terminalId = getTerminalId()
getJobFairs(terminalId ? { terminalId } : undefined)
```

所有本任务页面移除旧 helper import，`ProtoPage/ProtoBadge/ProtoNotice` 改为 W1 frame/header/state/action 与语义化 section，`ProtoStepStrip`/`ProtoListSteps` 改为带序号的 `<ol>`，`SourceMetaChips` 改为读取相同 sourceName/syncTime/externalId 的 `<dl>`；不修改范围外的历史 `jobs-fairs-prototype.tsx`。

保留地区、日期、日历、分页/列表行为和本校优先；预约只使用 fair 的来源 URL，终端 `扫码预约`、非终端 `去来源平台预约`，记录 `external_appointment`。

`JobFairCheckinPage` 继续调用真实 `getJobFairs({ terminalId: getTerminalId() })` 并只展示有 `fair.checkinUrl` 的活动；CTA 固定为 `扫码前往来源平台签到`，提示“本系统不记录签到结果”，记录 `external_checkin_open`。不得创建签到成功状态。

- [ ] **Step 3: 迁移详情与参展企业**

详情保留 `getJobFairById/getFairCompanies/getFairStats/getFairZones`、浏览上报、预约/签到来源上报、导航至 companies/map/materials/visit-plan/stats。统计 tab 的唯一真实数据分支为：

```tsx
stats && !stats.isMockData
  ? <FairDataScreen stats={stats} />
  : <KioskStatePanel tone="empty" title="暂无真实统计" description="现场统计尚未接入或仍在核验" />
```

`FairCompanyDetailPage` 保留来源岗位二维码与 `recordExternalJump(..., 'fair_company', company.id, 'external_apply')`；不得显示 AI 匹配分、录用率或平台投递。

- [ ] **Step 4: 验证并提交**

```bash
pnpm --filter @ai-job-print/kiosk verify:jobfair-ui
pnpm --filter @ai-job-print/kiosk verify:jobfair-commercial-closure
pnpm --filter @ai-job-print/kiosk verify:jobfair-checkin
node apps/kiosk/scripts/verify-jobfairs-terminal-priority.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
```

Expected: 全部 PASS，主详情文件不因迁移超过 500 行。

```bash
git add apps/kiosk/src/pages/job-fairs/JobFairsPage.tsx \
  apps/kiosk/src/pages/job-fairs/JobFairCheckinPage.tsx \
  apps/kiosk/src/pages/job-fairs/JobFairDetailPage.tsx \
  apps/kiosk/src/pages/job-fairs/FairCompaniesPage.tsx \
  apps/kiosk/src/pages/job-fairs/FairCompanyDetailPage.tsx \
  apps/kiosk/src/pages/job-fairs/components/FairCompanyDetailSections.tsx \
  apps/kiosk/src/pages/job-fairs/components/JobFairDetailTabs.tsx \
  apps/kiosk/src/pages/job-fairs/components/FairCalendarPopover.tsx \
  apps/kiosk/src/pages/job-fairs/components/RegionPicker.tsx \
  apps/kiosk/src/pages/styles/job-fairs-fusion.css
git commit -m "feat(kiosk): migrate fusion job fair discovery pages"
```

Expected: 10 files committed。

## Task 6: 迁移招聘会地图、资料、参会准备单与统计页

**Files:**
- Modify: `apps/kiosk/src/pages/job-fairs/FairMapPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/FairMaterialsPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/FairVisitPlanPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/FairStatsPage.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/components/MapBlock.tsx`
- Modify: `apps/kiosk/src/pages/job-fairs/components/FairDataScreen.tsx`
- Modify: `apps/kiosk/src/pages/styles/job-fairs-fusion.css`

- [ ] **Step 1: 迁移四个子页的统一状态**

每页使用 `KioskPageFrame`/`KioskPageHeader`，并给 loading/error/empty 明确 `KioskStatePanel`。地图只消费现有经纬度/场馆图，不新增地图 key 或外部依赖；地图不可用时展示地址与“场馆导览暂不可用”。

四页及 `MapBlock`/`FairDataScreen` 删除旧 helper import；`CardHead` 改为语义化 `<section aria-labelledby>` 标题块，`SourceMetaChips` 改为读取同一 DTO 字段的 `<dl>`，步骤说明使用 `<ol>`。不得为了视觉迁移丢掉来源机构、同步时间或外部编号。

- [ ] **Step 2: 锁住真实打印 URL**

`FairMaterialsPage` 的打印 handler 必须保持：

```ts
const printable = await prepareFairMaterialPrint(fairId, material.id)
if (!printable.printFileUrl) throw new Error('打印链接未就绪')
navigate('/print/confirm', {
  state: {
    file: {
      name: printable.filename,
      size: formatSize(Math.max(1, Math.round(printable.sizeBytes / 1024))),
      pages: printable.pageCount > 0 ? printable.pageCount : null,
      fileId: printable.fileId,
      fileUrl: printable.printFileUrl,
      mimeType: printable.mimeType,
    },
    params: makePrintParams({
      copies: 1,
      duplex: printable.pageCount > 1 ? 'double' : 'single',
      color: 'bw',
    }),
  },
})
```

不得把 `material.fileUrl`、外部签名 URL 或来源页面 URL 交给打印流程。

`FairVisitPlanPage` 保留真实 generate/poll/print 状态机；只有后端返回的 `file.printFileUrl` 才能进入打印。生成中使用 `tone="loading"`，后端拒绝 mock 时展示真实不可用状态，不伪造建议单。

- [ ] **Step 3: 锁住统计空态并控制文件体积**

`FairStatsPage` 遇 `stats.isMockData` 立即返回 `KioskStatePanel tone="empty"`，不能让 `FairDataScreen` 接到 mock。`FairDataScreen` 只渲染 DTO 中存在的聚合字段，不推算求职者画像或录用概率。

```bash
pnpm --filter @ai-job-print/kiosk verify:jobfair-commercial-closure
node apps/kiosk/scripts/verify-jobfair-page-size.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
```

Expected: 全部 PASS；`CampusPage.tsx`、`JobFairDetailPage.tsx`、`FairCompanyDetailPage.tsx` 均不超过 500 行。

- [ ] **Step 4: 提交**

```bash
git add apps/kiosk/src/pages/job-fairs/FairMapPage.tsx \
  apps/kiosk/src/pages/job-fairs/FairMaterialsPage.tsx \
  apps/kiosk/src/pages/job-fairs/FairVisitPlanPage.tsx \
  apps/kiosk/src/pages/job-fairs/FairStatsPage.tsx \
  apps/kiosk/src/pages/job-fairs/components/MapBlock.tsx \
  apps/kiosk/src/pages/job-fairs/components/FairDataScreen.tsx \
  apps/kiosk/src/pages/styles/job-fairs-fusion.css
git commit -m "feat(kiosk): migrate fusion job fair service pages"
```

Expected: 7 files committed。

## Task 7: 迁移校园招聘专区并诚实收口两个直达容错页

**Files:**
- Modify: `apps/kiosk/src/pages/campus/CampusPage.tsx`
- Modify: `apps/kiosk/src/pages/campus/components/CampusTabs.tsx`
- Modify: `apps/kiosk/src/pages/placeholders/CampusWelcomePage.tsx`
- Modify: `apps/kiosk/src/pages/placeholders/FreshmanInsightsPage.tsx`
- Modify: `apps/kiosk/src/pages/styles/campus-policy-fusion.css`

- [ ] **Step 1: 迁移 `/campus` 而不复制 smart-campus 逻辑**

`CampusPage` 保留以 `getJobFairs(terminalId ? { terminalId } : undefined)` 找校园招聘会，再加载 companies/stats/zones；保留 `recordBrowse(..., 'job_fair')` 与来源预约 `external_appointment`。`CampusTabs` 保留招聘会概览、参展单位、区域、统计等真实内容，mock stats 使用 empty panel。

不得 import `useSmartCampusConfig`、`ToolboxLaunchModals` 或智慧校园 service；校园招聘页面不展示迎新办理、校园卡或大数据能力。

- [ ] **Step 2: 替换两个三行 placeholder**

`CampusWelcomePage.tsx` 使用静态诚实容错页：

```tsx
<KioskPageFrame
  header={<KioskPageHeader title="校园招聘迎新指引" onBack={() => navigate('/campus')} backLabel="返回校园招聘" />}
>
  <KioskStatePanel
    tone="empty"
    title="当前没有独立迎新招聘内容"
    description="请返回校园招聘专区查看本校招聘会与来源平台信息；智慧校园迎新服务位于独立专区。"
    actions={<Button onClick={() => navigate('/campus')}>返回校园招聘</Button>}
  />
</KioskPageFrame>
```

`FreshmanInsightsPage.tsx` 使用同样壳层，title 为“校园招聘数据”，description 明确“当前没有经核验的校园招聘聚合统计，请查看具体招聘会；不会展示示例数据”，只返回 `/campus`。不得重定向或复制 `/smart-campus/freshman-insights`。

- [ ] **Step 3: 验证并提交**

```bash
pnpm --filter @ai-job-print/kiosk verify:jobfair-commercial-closure
node apps/kiosk/scripts/verify-jobfair-page-size.mjs
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
git add apps/kiosk/src/pages/campus/CampusPage.tsx \
  apps/kiosk/src/pages/campus/components/CampusTabs.tsx \
  apps/kiosk/src/pages/placeholders/CampusWelcomePage.tsx \
  apps/kiosk/src/pages/placeholders/FreshmanInsightsPage.tsx \
  apps/kiosk/src/pages/styles/campus-policy-fusion.css
git commit -m "feat(kiosk): migrate fusion campus recruitment pages"
```

Expected: 5 files committed；两个直达页无 `待开发`，campus/smart-campus 没有同义重定向。

## Task 8: 迁移智慧校园并保持扩展应用与未授权统计冻结

**Files:**
- Modify: `apps/kiosk/src/pages/smart-campus/SmartCampusHomePage.tsx`
- Modify: `apps/kiosk/src/pages/smart-campus/SmartCampusWelcomePage.tsx`
- Modify: `apps/kiosk/src/pages/smart-campus/SmartCampusServicePage.tsx`
- Modify: `apps/kiosk/src/pages/smart-campus/FreshmanInsightsPage.tsx`
- Modify: `apps/kiosk/src/pages/styles/campus-policy-fusion.css`

- [ ] **Step 1: 迁移首页但保留冻结共享 consumer**

使用 W1 primitives 替换 `kproto` page shell，保留 `useSmartCampusConfig`、module gating、`extensionItems.length > 0` 和三种 launch mode。以下代码语义必须原样存在：

```tsx
import { ExternalLaunchModal, QrLaunchModal } from '../home/components/ToolboxLaunchModals'
<QrLaunchModal item={qrItem} placement="smart_campus" onClose={() => setQrItem(null)} />
<ExternalLaunchModal item={externalItem} placement="smart_campus" onClose={() => setExternalItem(null)} />
```

四页均移除 `../prototype/kiosk-prototype.css` 的直接 import；兼容聚合入口仍保留到 W6。

不得复制弹窗实现，不得修改共享文件；无扩展项时不渲染扩展区，disabled 配置不能变成可启动。

- [ ] **Step 2: 迁移 welcome/service/insights 状态**

welcome/service 只展示后端配置允许的办事指引，不宣称已在线办理。`FreshmanInsightsPage` 固定使用：

```tsx
<KioskStatePanel
  tone="permission"
  title="校园大数据暂未开放"
  description="学校书面授权、数据处理协议与聚合脱敏统计三项条件全部满足后才会开放；开放前不展示统计，也不采集个人信息。"
  actions={<Button onClick={() => navigate('/smart-campus')}>返回智慧校园</Button>}
/>
```

页面不能 import 数据 service，不能出现数字卡、图表、专业分布、年龄分布或示例统计。

- [ ] **Step 3: 验证并提交**

```bash
pnpm --filter @ai-job-print/kiosk verify:smart-campus-ui
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
git diff --name-only b19d71f5 -- apps/kiosk/src/pages/home/components
```

Expected: smart-campus verifier PASS；最后一条无输出；W4 verifier 只剩 renshi/最终浏览器项。

```bash
git add apps/kiosk/src/pages/smart-campus/SmartCampusHomePage.tsx \
  apps/kiosk/src/pages/smart-campus/SmartCampusWelcomePage.tsx \
  apps/kiosk/src/pages/smart-campus/SmartCampusServicePage.tsx \
  apps/kiosk/src/pages/smart-campus/FreshmanInsightsPage.tsx \
  apps/kiosk/src/pages/styles/campus-policy-fusion.css
git commit -m "feat(kiosk): migrate fusion smart campus pages"
```

Expected: 5 files committed。

## Task 9: 迁移人社政策信息页并保持官方入口真实性

**Files:**
- Modify: `apps/kiosk/src/pages/renshi/RenshiPage.tsx`
- Modify: `apps/kiosk/src/pages/renshi/components.tsx`
- Modify: `apps/kiosk/src/pages/renshi/PolicyPanel.tsx`
- Modify: `apps/kiosk/src/pages/renshi/SocialPanel.tsx`
- Modify: `apps/kiosk/src/pages/renshi/RegisterPanel.tsx`
- Modify: `apps/kiosk/src/pages/renshi/NoticePanel.tsx`
- Modify: `apps/kiosk/src/pages/styles/campus-policy-fusion.css`

- [ ] **Step 1: 先跑政策真实性基线**

```bash
pnpm --filter @ai-job-print/kiosk verify:renshi-policy-ui
```

Expected: PASS。

- [ ] **Step 2: 只迁移呈现，不改变数据分类与记录条件**

`RenshiPage` 使用 W1 frame/header/state；保留 policy/notice/social/register 四 tab、audience 筛选、query 深链和一次真实 `getPublishedPolicies`。loading/error 用 `KioskStatePanel`；error 只重试，不 fallback 假政策。

以下逻辑不改：

```ts
const isBuiltin = (id: string) => id.startsWith('builtin-')
if (isBuiltin(item.id)) return
if (!isBuiltin(item.id)) recordExternalJump(getToken(), 'policy', item.id, 'external_open')
```

内置条目仍隐藏收藏；库内 published 条目保留收藏、浏览与外跳。官方入口弹层可换 `KioskModal`，但 URL 必须来自既有数据；内置 URL 的 `https` + `gov.cn` 规则继续由现有 verifier 校验。

- [ ] **Step 3: 保留信息服务边界并提交**

所有 `/print/upload` 按钮必须在同一 button 内展示“上传自备材料打印”或数据定义的扫码标签；不出现虚构材料名、页数、代办、保证到账或免申即享。

```bash
pnpm --filter @ai-job-print/kiosk verify:renshi-policy-ui
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
git diff --name-only b19d71f5 -- \
  apps/kiosk/src/pages/renshi/builtinData.ts \
  apps/kiosk/src/pages/renshi/shared.ts
```

Expected: 全部 PASS；最后一条无输出；此时静态 W4 verifier 首次 ALL PASS。

```bash
git add apps/kiosk/src/pages/renshi/RenshiPage.tsx \
  apps/kiosk/src/pages/renshi/components.tsx \
  apps/kiosk/src/pages/renshi/PolicyPanel.tsx \
  apps/kiosk/src/pages/renshi/SocialPanel.tsx \
  apps/kiosk/src/pages/renshi/RegisterPanel.tsx \
  apps/kiosk/src/pages/renshi/NoticePanel.tsx \
  apps/kiosk/src/pages/styles/campus-policy-fusion.css
git commit -m "feat(kiosk): migrate fusion policy information pages"
```

Expected: 7 files committed。

## Task 10: 添加真实 HTTP envelope fixture 与 W4 Playwright

**Files:**
- Create: `apps/kiosk/playwright.w4.config.ts`
- Create: `apps/kiosk/tests/fixtures/fusion-w4-api.ts`
- Create: `apps/kiosk/tests/visual/fusion-w4.spec.ts`
- Modify: `apps/kiosk/scripts/verify-fusion-w4.mjs`

- [ ] **Step 1: 创建独立 Playwright config，不修改 package script**

`playwright.w4.config.ts` 必须沿用 W1 的 proxy bypass，并设置：

```ts
export default defineConfig({
  testDir: './tests',
  testMatch: /fusion-w4\.spec\.ts$/,
  outputDir: '../../test-results/kiosk-fusion-w4',
  fullyParallel: false,
  forbidOnly: Boolean(process.env.CI),
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: process.env.CI
    ? [['line'], ['html', { outputFolder: '../../test-results/kiosk-fusion-w4-report', open: 'never' }]]
    : 'list',
  use: {
    baseURL: 'http://127.0.0.1:4184',
    locale: 'zh-CN',
    timezoneId: 'Asia/Shanghai',
    colorScheme: 'light',
    contextOptions: { reducedMotion: 'reduce' },
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
    video: 'off',
    viewport: { width: 1080, height: 1920 },
  },
  webServer: {
    command: 'VITE_API_MODE=http VITE_API_BASE_URL=/api/v1 VITE_USE_TRTC_CALL=true VITE_ALLOW_TEXT_ONLY_ASSISTANT=false VITE_TERMINAL_ID=KSK-001 pnpm build && pnpm exec vite preview --host 127.0.0.1 --port 4184 --strictPort',
    url: 'http://127.0.0.1:4184',
    reuseExistingServer: false,
    timeout: 180_000,
  },
})
```

- [ ] **Step 2: 创建结构锁定的真实 fixture 注册器**

`fusion-w4-api.ts` 导出 `registerW4Api(api: ApiRouter)`，使用各 HTTP adapter 实际接收的 wire/DTO 字段与真实 envelope，不在页面加 demo 开关。`WireFairCompany`/`WireFairZone` 是生产 adapter 的私有 interface，测试不得声称直接获得编译期安全；fixture 定义完整镜像（含嵌套 `WireFairPosition`）并用 `satisfies` 锁住数据。Task 10 同步修改 `verify-fusion-w4.mjs`，以 TypeScript AST 对生产 interface 与测试镜像执行属性名、required/optional、数组/标量及 primitive/reference type 的 exact equality；fixture/mirror 缺失必须 fail closed。该 drift 检查在 Task 10 才加入，因此 Task 9 的“首次 ALL PASS”只指 runtime presentation contract，Task 10 后才包含 fixture contract。不能直接把页面层 `FairCompanyDTO` 冒充服务端 wire response。至少注册：

```ts
api.respond('GET', '/api/v1/jobs', {
  status: 200,
  json: { success: true, data: [job], pagination: { page: 1, pageSize: 100, total: 1, totalPages: 1 } },
})
api.respond('GET', '/api/v1/jobs/job-001', {
  status: 200,
  json: { success: true, data: job },
})
api.respond('GET', '/api/v1/kiosk/offline-agencies', {
  status: 200,
  json: { success: true, data: { items: [agency], total: 1, page: 1, pageSize: 10, stats: { totalAgencies: 1, openAgencies: 1, totalJobs: 2, districts: 1, lastSyncLabel: '今日' } } },
})
api.respond('GET', '/api/v1/companies', { status: 200, json: { success: true, data: { items: [company], nextCursor: null, total: 1 } } })
api.respond('GET', '/api/v1/companies/stats', { status: 200, json: { success: true, data: companyStats } })
api.respond('GET', '/api/v1/companies/company-001', { status: 200, json: { success: true, data: companyDetail } })
api.respond('GET', '/api/v1/companies/company-001/jobs', { status: 200, json: { success: true, data: { items: [companyJob], nextCursor: null, total: 1 } } })
api.respond('GET', '/api/v1/job-fairs', { status: 200, json: { success: true, data: [fair], pagination: { page: 1, pageSize: 20, total: 1, totalPages: 1 } } })
api.respond('GET', '/api/v1/job-fairs/fair-001', { status: 200, json: { success: true, data: fair } })
api.respond('GET', '/api/v1/job-fairs/fair-001/companies', { status: 200, json: { success: true, data: [fairCompany], pagination: { page: 1, pageSize: 20, total: 1, totalPages: 1 } } })
api.respond('GET', '/api/v1/job-fairs/fair-001/zones', { status: 200, json: { success: true, data: [fairZone] } })
api.respond('GET', '/api/v1/job-fairs/fair-001/stats', { status: 200, json: { success: true, data: { ...fairStats, isMockData: true } } })
api.respond('GET', '/api/v1/terminals/KSK-001/config', { status: 200, json: terminalConfig })
api.respond('GET', '/api/v1/terminals/KSK-001/screensaver', {
  status: 200,
  json: { enabled: false, idleTimeoutSec: 180, items: [] },
})
api.respond('GET', '/api/v1/terminals/KSK-001/printer-status', {
  status: 200,
  json: { printerStatus: 'ready', paperLevel: 'sufficient', isOnline: true },
})
api.respond('GET', '/api/v1/policies', { status: 200, json: { success: true, data: [publishedPolicy] } })
```

fixture 值使用可识别但非个人信息的名称，例如“前端工程师”“青岛示例制造有限公司”“2026 青岛高校毕业生招聘会”；source URL 使用 `https://jobs.example.gov.cn/...`，checkin/appointment URL 使用 HTTPS。不得含 token、手机号、签名 URL、真实用户文档或 prompt。

`ApiRouter` 已对未注册 `/api/v1/**` 请求 abort，并在用例结束 `assertNoUnhandledRequests()`；W4 不修改它，也不允许 `page.route('**/*', route.continue)` 绕过 fail-closed。

- [ ] **Step 3: 写代表性 Playwright 用例**

`fusion-w4.spec.ts` 必须 import `test/expect` from `../fixtures/kiosk-test`、`registerW4Api`、`assertNoHorizontalOverflow`，并逐项覆盖：

```ts
test('/jobs 保留线上与线下双轨 @w4', async ({ page, api }) => {
  registerW4Api(api)
  await page.goto('/jobs')
  await expect(page.getByText('前端工程师')).toBeVisible()
  await expect(page.getByRole('button', { name: /线下机构门店/ })).toBeVisible()
  await assertNoHorizontalOverflow(page)
})

test('/jobs/:id 只提供来源 CTA @w4', async ({ page, api }) => {
  registerW4Api(api)
  await page.goto('/jobs/job-001')
  await expect(page.getByText('信息以来源平台为准')).toBeVisible()
  await expect(page.getByRole('button', { name: '扫码投递' })).toBeVisible()
  await expect(page.getByText(/一键投递|立即投递/)).toHaveCount(0)
})

test('/offline-agencies 不导航到不存在的详情 @w4', async ({ page, api }) => {
  registerW4Api(api)
  await page.goto('/offline-agencies')
  await expect(page.getByText('青岛合规人力服务机构')).toBeVisible()
  await expect(page.locator('a[href^="/offline-agencies/"]')).toHaveCount(0)
})

test('/job-fairs 预约离开平台且 mock 统计为空 @w4', async ({ page, api }) => {
  registerW4Api(api)
  await page.goto('/job-fairs/fair-001')
  await expect(page.getByRole('button', { name: /扫码预约|去来源平台预约/ }).first()).toBeVisible()
  await page.getByRole('button', { name: '数据大屏' }).click()
  await expect(page.getByText(/暂无真实统计/)).toBeVisible()
  await expect(page.getByText(/签到成功|确认签到/)).toHaveCount(0)
})

test('/campus 与 /smart-campus 语义独立 @w4', async ({ page, api }) => {
  registerW4Api(api)
  await page.goto('/campus')
  await expect(page.getByText(/校园招聘/).first()).toBeVisible()
  await page.goto('/smart-campus/freshman-insights')
  await expect(page.getByText('校园大数据暂未开放')).toBeVisible()
  await expect(page.getByText(/学校书面授权/)).toBeVisible()
})

test('/renshi 官方信息不承诺代办 @w4', async ({ page, api }) => {
  registerW4Api(api)
  await page.goto('/renshi')
  await expect(page.getByText(/以官方发布为准/).first()).toBeVisible()
  await expect(page.getByText(/保证到账|免申即享/)).toHaveCount(0)
})
```

`registerW4Api` 接受 `{ smartCampusEnabled?: boolean }`，据此为每个新 browser context 构造 enabled/disabled 的 `terminalConfig`，不能靠跨用例模块缓存切换状态。另加 companies 列表/详情、job-fairs checkin、campus 两个容错页、smart-campus disabled/home/service 的场景。每个用例收集 `pageerror`，最后断言 `[]`；每页执行 `assertNoHorizontalOverflow`。

- [ ] **Step 4: 运行浏览器 GREEN 并提交**

```bash
pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts --list
pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts
CI=1 pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts
HTTP_PROXY=http://127.0.0.1:9 HTTPS_PROXY=http://127.0.0.1:9 ALL_PROXY=http://127.0.0.1:9 NO_PROXY= no_proxy= pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts
```

Expected: list 只收集 W4 spec；normal、CI 单 worker、broken-proxy 三环境 Chromium 1080×1920 全部 PASS；无未注册 API、pageerror、水平溢出；失败截图只写 `test-results/` 且不提交。

```bash
git add apps/kiosk/playwright.w4.config.ts \
  apps/kiosk/tests/fixtures/fusion-w4-api.ts \
  apps/kiosk/tests/visual/fusion-w4.spec.ts \
  apps/kiosk/scripts/verify-fusion-w4.mjs
git commit -m "test(kiosk): cover fusion w4 browser flows"
```

Expected: 4 files committed；fixture drift contract 也已进入 ALL PASS。

## Task 11: 全量回归、scope 审计与 W4 交付检查

**Files:**
- No production file changes expected.

- [ ] **Step 1: 跑 W4 全部静态与行为门禁**

```bash
node apps/kiosk/scripts/verify-fusion-w4.mjs
pnpm --filter @ai-job-print/kiosk verify:job-info-ui
pnpm --filter @ai-job-print/kiosk verify:job-ai-ui
pnpm --filter @ai-job-print/kiosk verify:job-ai-history-privacy-ui
pnpm --filter @ai-job-print/kiosk verify:jobfair-ui
pnpm --filter @ai-job-print/kiosk verify:jobfair-commercial-closure
pnpm --filter @ai-job-print/kiosk verify:jobfair-checkin
node apps/kiosk/scripts/verify-jobfairs-terminal-priority.mjs
node apps/kiosk/scripts/verify-jobfair-page-size.mjs
pnpm --filter @ai-job-print/kiosk verify:smart-campus-ui
pnpm --filter @ai-job-print/kiosk verify:renshi-policy-ui
```

Expected: 每条输出 PASS/ALL PASS；W4 verifier 报告精确 23 routes；页面尺寸门禁全部 ≤500 行。

- [ ] **Step 2: 跑 W1 foundation、工程与生产配置门禁**

```bash
node packages/ui/scripts/verify-fusion-youth-foundation.mjs
node apps/kiosk/scripts/verify-fusion-shell.mjs
node apps/kiosk/scripts/verify-fusion-home.mjs
pnpm --filter @ai-job-print/kiosk typecheck
pnpm --filter @ai-job-print/kiosk lint
VITE_API_MODE=http \
VITE_API_BASE_URL=/api/v1 \
VITE_USE_TRTC_CALL=true \
VITE_ALLOW_TEXT_ONLY_ASSISTANT=false \
VITE_TERMINAL_ID=KSK-001 \
pnpm --filter @ai-job-print/kiosk build
VITE_API_MODE=http \
VITE_API_BASE_URL=/api/v1 \
VITE_USE_TRTC_CALL=true \
VITE_ALLOW_TEXT_ONLY_ASSISTANT=false \
VITE_TERMINAL_ID=KSK-001 \
pnpm --filter @ai-job-print/kiosk verify:prod-build-config
```

Expected: foundation/shell/home PASS；typecheck 0 errors；lint 0 errors；production build 完成；config guard PASS。构建变量只记录名称与布尔选择，不记录密钥值。

- [ ] **Step 3: 复跑 W4 浏览器与 diff 检查**

```bash
pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts --list
pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts
CI=1 pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts
HTTP_PROXY=http://127.0.0.1:9 HTTPS_PROXY=http://127.0.0.1:9 ALL_PROXY=http://127.0.0.1:9 NO_PROXY= no_proxy= pnpm --filter @ai-job-print/kiosk exec playwright test --config=playwright.w4.config.ts
git diff --check b19d71f5
git diff --name-only b19d71f5 -- \
  services packages/shared apps/kiosk/src/services apps/kiosk/src/routes \
  apps/kiosk/package.json .github/workflows/ci.yml apps/kiosk/src/index.css
git diff --name-only b19d71f5 -- \
  apps/kiosk/src/components/ComingSoonNotice.tsx \
  apps/kiosk/src/pages/home/components/ToolboxLaunchModals.tsx \
  apps/kiosk/src/pages/home/components/kioskAppLaunch.ts \
  apps/kiosk/src/pages/jobs/utils/jobDisplay.ts \
  apps/kiosk/src/pages/renshi/builtinData.ts \
  apps/kiosk/src/pages/renshi/shared.ts
git status --short
```

Expected: W4 list 范围正确且三环境 Playwright PASS；`git diff --check` 无输出；两条禁区 diff 无输出；status 仅显示明确属于其他 wave 的未跟踪计划时可继续，不能把它们加入 W4 commit。

- [ ] **Step 4: 核对每个 commit 文件预算**

```bash
git log --format='%h %s' b19d71f5..HEAD
for commit in $(git rev-list --reverse b19d71f5..HEAD); do
  count=$(git diff-tree --no-commit-id --name-only -r "$commit" | wc -l | tr -d ' ')
  printf '%s %s\n' "$count" "$(git show -s --format='%h %s' "$commit")"
done
```

Expected: 每个实现 commit 的 count ≤12；提交主题依次覆盖 contract、CSS、jobs、companies、fair discovery、fair services、campus、smart-campus、policy、browser。

- [ ] **Step 5: 仅在修复验证失败时提交收尾**

若以上验证触发修复，只修改对应 W4-owned 文件，重跑受影响命令与全量 Step 1–3。以 `git status --short` 取得本次实际修改清单，逐个核对均在 W4 allowlist 后显式暂存，例如修复 `JobFairsPage.tsx` 时：

```bash
git add apps/kiosk/src/pages/job-fairs/JobFairsPage.tsx
git diff --cached --name-only
git commit -m "fix(kiosk): close fusion w4 regressions"
```

Expected: cached 清单只含已核对的 W4-owned 修复；修复 commit ≤12 files；全量回归重新 PASS。若修复涉及其他文件，将每个实际路径显式追加到同一 `git add`，不得使用目录级或通配暂存。若没有修复，不创建空提交，也不修改进度文档；W6 集成阶段统一更新项目进度与全路由验收记录。
