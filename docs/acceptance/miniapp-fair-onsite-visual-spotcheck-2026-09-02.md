# 小程序新页面视觉抽查清单（批 1 + 批 2，共 9 页）

> 状态：待执行。9 个新页面已过静态门禁 110 项、API 契约门禁、视觉刻度棘轮门禁，
> 并做过多模型只读代码评审，但**没有任何人在微信开发者工具或真机上看过一眼**。
>
> 逻辑、合规、数据契约由门禁盯着；**真机唯一能推翻的是版式与渲染**。
> 本清单只解决一件事：有没有系统性版式问题。单页瑕疵之后逐条修，不影响继续做下一批。

本轮 9 页：`fair-venue` / `fair-companies` / `fair-company-detail` / `fair-materials` /
`fair-visit-plan` / `policy-check` / `self-explore` / `job-materials` / `resume-build`

---

## 0. 先读这段：今天能验什么

生产库里**岗位 0 条、招聘会 0 条、政策 0 条、公开终端 0 个**
（只读探测记录：`docs/delivery/miniapp-fair-onsite-2026-09/evidence/EV-009-real-backend-readonly-probe.md`）。

**所以招聘会相关的 5 个页面今天打不开任何内容——那不是页面坏了，是库里没东西。**

| 页面 | 今天能走到哪 | 卡在哪 |
|---|---|---|
| `job-materials` 求职材料模板 | **全流程**：6 张模板卡 → 填写页 5 个字段 | 只有点「生成」需要登录 |
| `policy-check` 政策条件自测 | **全流程**：9 个问项 → 比对 → 结果 | 结果必然是空态（库里没有可比对条件的政策） |
| `self-explore` 自我探索 | 同意书 → 25 题分 5 组 → 提交 | **雷达图在结果页，要登录 + 等 AI 跑几十秒** |
| `resume-build` 从零建简历 | **6 段表单全部**（本清单重点之一） | 只有点「提交生成」需要登录 |
| `fair-venue` 会场导览 | 只有空态（见 G 项） | 无招聘会 |
| `fair-companies` 参会企业 | 只有空态（见 G 项） | 无招聘会 |
| `fair-company-detail` 企业详情 | 打不开 | 无企业 |
| `fair-materials` 活动资料 | 只有空态（见 G 项） | 无招聘会 |
| `fair-visit-plan` AI 参会准备单 | 只有「没有简历」引导态 | 无招聘会 |

**今天能真验的是前 4 页。** 招聘会那 5 页只在第 G 项走一遍空态和外壳，不要在上面花时间。

---

## 1. 前置条件

- [ ] 微信开发者工具打开：`/Users/wanglei/AI求职打印服务终端-miniapp/apps/miniapp`
      （**不是**主 checkout 的 `~/AI求职打印服务终端/apps/miniapp`）
- [ ] 编译通过，问题面板无新增 Error
- [ ] 「详情 → 本地设置」勾选「不校验合法域名」（仅本地调试）
- [ ] 模拟器机型选 **iPhone SE / 375pt 宽**——窄屏才暴露折行、截断和溢出
- [ ] 右上角「普通编译」下拉 →「添加编译模式」，新建一条：
      启动页面 `pages/resume-build/resume-build`，不带参数。
      **`resume-build` 全仓没有任何入口按钮**（只有「AI 服务记录」里的历史条目指向它，
      而在你生成第一份之前那里是空的），必须用编译模式才能打开。这是已知的，不用报。

**不需要登录的**：A / B / C / D / F / G 项全部。
**需要登录的**：只有 E 项（雷达图）。登录路径是首页未登录时的启动页，或「我的」里的登录入口，
短信验证码登录走真实后端。如果登录拿不到，E 项就跳过并直接告诉我——**E 项是本轮最重要的一项，
跳过了要单独记一笔，不能当成「验过了」。**

---

## 2. 抽查项（7 项）

9 页按版式只有 4 类原型（列表 / 详情 / 表单 / AI 结果长页），同类页共用同一套全局类
（`navbar` / `card` / `chip` / `empty` / `list-state` / `actionbar` / `btn`），
系统性问题会同时出现在所有同类页上，抽一个代表就够。
**canvas 雷达图和 6 段表单是本轮新引入的原型，各占一项，不能省。**

---

### A. 共享外壳 —— 打开「政策条件自测」

路径：底部 Tab「求职」→ 顶部筛选条最右的「政策」→ 首屏那张
「看看哪些政策可能和我有关」卡片。

- [ ] 进政策服务页后，**快速双击**「看看哪些政策可能和我有关」那张卡：
      只应压进一层页面，**返回一次就回到政策列表**（要按两次返回 = 不通过）
- [ ] 顶部返回箭头位置正常，标题「政策条件自测」不被微信右上角胶囊（··· ⊙）遮挡
- [ ] 页面滚到底，最后一块内容不被底部操作条吃掉
- [ ] 底部按钮「开始比对（已选 N 项）」高度、圆角、字号和你熟悉的其他页一致

> **A 项出问题 = 全局壳层出事，后面六项可以不看，直接告诉我。**

---

### B. 卡片内边距 —— 同一页，「政策条件自测」+「求职材料模板」

这一项我**预判会不通过**，需要你确认我读得对不对，只要 5 秒。

我按代码算：全局 `.card` 只有背景、圆角、边框、外边距，**没有内边距**；内边距来自
`.card-inner`。而 `policy-check` 的 9 个问项卡和 `job-materials` 的模板卡、字段卡
**都直接用了 `.card`，没有套 `.card-inner`**。

- [ ] 政策条件自测：9 个问项卡里，问题文字和下面的选项药丸**是不是贴着卡片左右边框**，
      左右完全没有留白
- [ ] 求职材料模板（「职业生活圈」→ 材料模板）：6 张模板卡的标题、描述、标签
      是不是同样贴边
- [ ] 对照组：自我探索的同意书卡片（「职业生活圈」→ 自我探索）左右有约 16px 留白。
      **两者留白明显不一样 = 不通过**

> 如果实际有留白，说明我的读法错了——**请告诉我**，这比确认我对更重要。

---

### C. 列表原型 —— 「求职材料模板」列表页

代表 `fair-companies` / `fair-materials` / `policy-check` 结果列表。今天有 6 条真实数据。

- [ ] 6 张卡竖排，卡与卡之间距离均匀，没有某两张挨得特别近或特别远
- [ ] 每张卡右上角的类型标签（求职信 / 感谢信 / 作品集封面 / 材料清单 / 简历模板）
      **和左边标题在同一行且不重叠**；标题长时应当换行而不是把标签挤出卡片
- [ ] 第一张「清爽通用简历模板」整体比其他 5 张淡一档（这是有意的），
      但**文字仍然看得清**，不是灰到读不出来
- [ ] 点第一张 → 弹窗说「简历请走简历诊断或优化」；点其他任意一张 → 切到填写页
      （这一跳是**同页换内容**，不压新页面，回列表用底部「换模板」）

---

### D. 表单原型 + 底部操作条 —— 「求职材料模板」填写页

代表 `policy-check` 问项和一切「字段卡 + 底部两键」的页。5 个字段：姓名、目标岗位、
目标单位、核心亮点（多行）、补充说明（多行）。

- [ ] 输入框宽度：**是不是和卡片一样宽、直接顶到卡片边框**（配合 B 项一起看）
- [ ] 在「核心亮点」里连打三行回车换行 —— 输入框应当**自己长高**，不出现内部滚动条，
      也不把下面的卡片盖住
- [ ] 已填内容后，把光标点回**文字中间**再敲一个字：
      **光标跳到末尾 = 不通过**（原生小程序受控输入的经典坑，实现方没验过）
- [ ] 底部两个按钮「换模板 / 生成并去打印」：
      **两枚按钮加起来没有铺满整条底栏、右侧空一截 = 不通过**（我算下来大概率会这样）

---

### E. canvas 五维雷达图 —— 「自我探索」结果页 ★本轮最重要

**这是全仓唯一一处 canvas 绘制，静态门禁完全覆盖不到。** 迄今只用桩验证过绘制调用
（按 pixelRatio 放大画布、五边形路径、5 处文字标注），**没有任何人看过它长什么样**。

路径：「职业生活圈」→ 自我探索 → 勾选同意 → 25 题分 5 组答完 → 提交（要登录，等几十秒）。

- [ ] 五边形**居中**，不偏左右，顶点朝正上方
- [ ] 五个顶点旁的文字标注（兴趣偏好 / 工作风格 / 团队偏好 / 价值取向 / 求职动机）
      **一个字都没被画布边缘切掉**。左右两个顶点最挤，重点看这两个
- [ ] 每个标注下面的「N/5」数字都看得见，不和维度名叠在一起
- [ ] **图形和卡片的关系**：雷达图左右两侧的留白，是不是明显比同一页
      「本次记录」卡片里文字的左右留白窄。**明显窄一截 = 不通过**
      （我算下来画布比卡片内容区宽约 18px，会挤掉一半留白）
- [ ] 五边形在卡片里是不是**明显偏小、下方留出一大片空白**。
      我算下来图形只占画布宽度的一半多一点，下方约 40px 是死区
- [ ] **数值一致**：图上五个顶点写的「N/5」，和图下方那份文字 + 条形列表里的
      五个数字**逐个对得上**（这两份是同一份数据的两种表达，对不上是硬缺陷）
- [ ] 如果某个维度是 0：那个顶点应当**收缩到正中心**，五边形变形。
      **不通过的样子是：0 的顶点仍然离中心有一段距离**（那等于把 0 画成非 0）

> 补一句更正：**雷达图本身不可点击、没有做放大预览**，代码里没有这个功能。
> 不要去点它然后报「点不动」。（`wx.previewImage` 是会场导览那张平面图的能力，见 G 项。）

---

### F. 6 段表单 —— 「从零建一份简历」 ★本轮第二重要

只能用第 1 节配置的编译模式打开。25+ 个输入框分 6 段（基本 / 意向 / 教育 / 经历 / 项目 / 自述），
顶部有步骤条。

- [ ] 步骤条：6 个圆圈 + 6 个 2 字标签（基本 意向 教育 经历 项目 自述）
      **在一行里排得开、不换行、不被挤出屏幕**，圆圈之间的连接线看得见
- [ ] 点步骤条上的任意一个圆圈能直接跳到那一段；当前段的圆圈是实心高亮
- [ ] 第 1 段填了姓名后翻到第 2 段再翻回来，第 1 段的圆圈变成「已填」样式（浅色实心），
      **内容还在**
- [ ] **光标跳位**（和 D 项同一个坑，但这一页字段最多、最容易触发）：
      在「姓名」里输入「张三丰」，把光标点回「张」和「三」之间，再敲一个字。
      **光标跳到末尾 = 不通过**。中文拼音输入法多打几个字也试一下
- [ ] 第 3 段「教育经历」→「补充说明」是自动长高的多行框：连打几行，
      框应当自己长高，且**长高时上面的「起止时间」输入框不跟着抖**
- [ ] 第 5 段「项目与技能」→ 技能框里打「Java、Python，React」，
      下面的标签应当**当场变成 3 个**，且标签行不溢出卡片
- [ ] 第 3/4/5 段点「添加一条」再点右上角 ×：应当**先弹确认框**再删
- [ ] 底部「上一段 / 下一段」两键：**主按钮明显比次按钮宽**，两键铺满底栏

---

### G. 招聘会 5 页 —— 只看空态和外壳，一次走完

这 5 页今天没有内容可看，**不要在这上面花超过 3 分钟**。从底部「求职」→ 招聘会，
列表本身就是空的，所以用编译模式逐个打开。

后端对「招聘会不存在 / 未发布」返回的是 **200 + 空集**（不是 404），所以
**编译模式的「启动参数」里填 `fairId=demo`** 就能看到真实的空态；不填参数只会看到
「缺少招聘会参数」这一句，什么版式都验不到。

| 编译模式启动页 | 启动参数 | 预期 |
|---|---|---|
| `pages/fair-companies/fair-companies` | `fairId=demo` | 「暂无参会企业」空态；搜索框和展区筛选条**不应出现** |
| `pages/fair-materials/fair-materials` | `fairId=demo` | 「没有可展示的活动资料」空态 |
| `pages/fair-venue/fair-venue` | `fairId=demo` | 一条「主办方未配置导览」提示；平面图、展厅、展位三块**整块不出现**（不该看到占位框或三连「暂无」） |
| `pages/fair-visit-plan/fair-visit-plan` | `fairId=demo` | 「还没有可用的简历」+「去上传简历」入口（不是报错） |
| `pages/fair-company-detail/fair-company-detail` | 不填 | 加载失败态即可，不必细看 |

- [ ] 5 页都**没有白屏、没有报错弹窗、没有「undefined」/「null」露到界面上**
- [ ] 空态图标、标题、说明三行居中，不贴边、不重叠
- [ ] 空态里的「重新加载」按钮够大（约 56px 高），不是一条细窄的文字
- [ ] 每页返回箭头都能正常退回

> 会场导览那张平面图的点击放大（`wx.previewImage`）**今天验不了**——没有招聘会就没有图。
> 那张图是短时签名 URL，预览时可能已失效，等库里有内容后单独验，本轮记为「未验」。

---

## 3. 通过标准

- **A 项不过** = 全局壳层出事 → 停下来，先修，后面别看了
- **B 项不过** = 两页的卡片缺内边距 → 是真缺陷但改动很小（加一层 `card-inner`），
  记下来，不阻塞下一批
- **E 项不过**（雷达图切字 / 数值对不上 / 0 顶点没收缩）= canvas 这条路要重做，
  **这是唯一一个可能需要推翻实现方案的项**
- **F 项光标跳位** = 6 段表单不可用，必须先修再谈别的
- C / D / G 的个别瑕疵 = 单页问题，记下来我逐条修
- **B–G 中有三项以上出同类问题**（比如到处都缺内边距、到处都溢出）= 系统性版式问题，
  停下来先收口本轮

---

## 4. 反馈方式

截图 + 一句话就够，不需要写报告。每一项只要给我三种结果之一：**过 / 不过 / 没验**。
「没验」也要说——把没验的当成验过的，比不验更贵。

最重要的三个判断，按重要性排：

1. E 项雷达图到底长什么样（这是全仓唯一没人看过的渲染）
2. F 项 6 段表单能不能正常打字
3. 有没有系统性版式问题

---

## 5. 已知的、不用你验的

以下都已经确认，**不需要你花时间，也不要作为缺陷报回来**：

**全局层面**

- 全局 `.navbar .nav-back` 返回键热区是 32×32px，低于 48px 触控下限。它被 59 个页面共用，
  只改这几页会造成返回键位置错位，本批未动。
- `resume-build` 没有 UI 入口，只能用编译模式打开（见第 1 节）。入口挂在哪一页
  还没定，等这一页验完再说。

**招聘会 5 页（等库里有内容后才验得了）**

- 「展位分布」区块恒为空态：服务端 `jobs-kiosk.service.ts` 把 `booths` 写死成空数组，
  链路先保留，等后端补齐。
- 活动资料的「累计被打印 N 次」永远不显示：后端没有出纸后递增的写路径，`printCount` 恒为 0，
  页面按 `> 0` 才渲染，所以看不到是**正确行为**。
- 会场导览平面图的 `wx.previewImage` 放大、`fair-visit-plan` 的 AI 生成长内容与状态机、
  参会企业的搜索 / 展区筛选 / 下拉刷新、企业详情底部三键——**全部本轮未验**，
  原因都是同一个：库里没有招聘会。这些已登记为待验，不是遗漏。

**打印与内容库**

- 打印按钮点下去之后的下单流程（`print-upload` 的报价与隐私检查）本批只做了代码级打通，
  同样没有真机验证。
- `/terminals/public` 返回空数组 → 打印流程里的终端选择今天没有可选项。
- 政策条件自测的结果**必然是空态**：库里没有任何录入了可机械比对条件的政策。
  页面在这种情况下说的是「没有可比对的政策，不代表你不符合任何政策」——
  **这句话是对的**，不要当成 bug。
- AI 长任务（`aiTimeout` 90 秒）的真实耗时与超时行为从未实测。E 项和 F 项提交时
  如果等很久，先等满 90 秒再判失败。
